# CCAR-F 学習ノート — 作業ガイド

このリポジトリを触る Claude 向けの引き継ぎ。**編集を始める前にこのファイルを最後まで読むこと。**
過去に実際に起きた失敗と、その再発防止策が「これまでに直した問題」に列挙してある。

---

## 1. これは何か

**CCAR-F（Claude Certified Architect – Foundations）合格**のための学習サイト。
利用者は日本語話者ひとり。**表記はすべて日本語**（コード・API のフィールド名を除く）。

出題ブループリント（5ドメイン・30タスク）をそのまま目次にしている。
「読み物として面白いか」より **「試験に出る内容を正しく理解できるか」を優先**する。
これは利用者が明示した方針。

| ドメイン | 比率 | ディレクトリ |
|---|---|---|
| 前提（全ドメイン共通の土台） | ─ | `docs/00-basics/` |
| Domain 1 エージェント設計とオーケストレーション | 27% | `docs/01-agentic/` |
| Domain 2 ツール設計と MCP 連携 | 18% | `docs/02-tools/` |
| Domain 3 Claude Code の設定とワークフロー | 20% | `docs/03-claude-code/` |
| Domain 4 プロンプト設計と構造化出力 | 20% | `docs/04-prompt/` |
| Domain 5 コンテキスト管理と信頼性 | 15% | `docs/05-context/` |
| まとめ・総合チェック | ─ | `docs/06-summary/` |

### 番号が2系統ある ─ 混同しないこと

| | 形 | 意味 |
|---|---|---|
| **タスク番号** | `1.1`（ドット） | 公式ブループリントの出題単位。全30個。本文では `BLUEPRINT 1.1` と表示する |
| **項番号** | `1-3`（ハイフン） | この教材の読む単位。ドメイン内の通し番号。`<h2>` の先頭に入れる |

1タスクを複数の項に割ることがある。逆に、どのタスクにも対応しない項（前提・導入・まとめ・模擬）もある。
この対応関係は `docs/index.html` の「公式ブループリントと、この教材の対応」で読み手に説明済み。

採点思想は **「効く一番単純な手」**。設問で凝った選択肢はたいてい誤答。教材全体がこの基準で書かれている。

---

## 2. ファイル構成

```
docs/                   ← 成果物。この中だけで完結して動く（GitHub Pages の /docs にそのまま出せる）
  index.html            トップ（ブループリントとの対応・読む順番・配点・ドメイン一覧）
  00-basics/            ドメインごとのディレクトリ
    01-flow.html          1ファイル＝1項。ファイル名は NN-<節のid>.html（NN は読む順）
    02-resend.html
    …
  01-agentic/ … 06-summary/
  assets/
    style.css           全スタイル。ダーク（既定）／ライトの2テーマ
    app.js              テーマ切替・目次開閉・クイズ描画・操作できる図・全文検索
    quiz-data.js        全設問データ（window.QUIZ）
    search-index.js     全文検索インデックス（自動生成物・手で編集しない）
  .nojekyll             GitHub Pages に Jekyll を通させないための空ファイル
tools/
  reindex.mjs           節ファイルの周りを書き出す ＋ 索引の再生成 ＋ ?v= の打ち直し
  check.mjs             静的チェック（過去の不具合の再発検出）
CLAUDE.md / README.md
```

全88項（本文64・理解度チェック24）。外部依存は **Google Fonts のみ**（Inter / Noto Sans JP / JetBrains Mono）。
オフラインでもシステムフォントにフォールバックして読める。ビルドもサーバも不要で、`docs/index.html` を開けば動く。

### 節ファイルの中身は「本文」だけ

head・上部ナビ・矢印defs・目次・パンくず・前後の送り・検索窓は、
`tools/reindex.mjs` のテンプレートが**唯一の出所**で、実行するたびに88ファイルへ書き出される。

```html
<!-- ▼ 本文 ─ ここだけを編集する。この前後は node tools/reindex.mjs が書き出す -->
<section class="sec" id="loop">…</section>
<!-- ▲ 本文 -->
```

**この目印の外を手で編集しても、次の `reindex` で消える。** 共通部を変えたいときは `reindex.mjs` を直す。
`docs/index.html` のカード・分量の数字も `<!--#cards-->` `<!--#volume-->` の中に生成されるので、手で書かない。

---

## 3. 編集の原則

**正典は各節ファイルの「本文」部分そのもの。** それ以外は生成物。

編集したら **必ずこの順で**：

```bash
node tools/reindex.mjs   # 検索インデックスを作り直す
node tools/check.mjs     # 静的チェック（exit 1 なら要修正）
```

`reindex` を忘れると検索が本文とズレる。`check` は過去に実際に埋め込んだバグを検出する。

`reindex.mjs` は4つのことをする。**HTML を書き換えるので、実行後の差分に HTML が含まれるのは正常**。

1. 各節ファイルの本文（`▼ 本文` 〜 `▲ 本文`）を読み取る
2. その前後（head・上部ナビ・矢印defs・目次・パンくず・前後の送り・検索窓）を書き出す
3. `assets/search-index.js` を本文から作り直す
4. `assets/*.css|js` 参照に `?v=<中身の SHA1 先頭8桁>` を打ち直す

4 はキャッシュ対策。これがないと、ブラウザが古い `search-index.js` や `style.css` を使い続けて、
本文と検索結果がズレる。**`<meta http-equiv="Cache-Control">` は使わない** —
ブラウザが確実に尊重せず、そもそも CSS / JS のような下位リソースには効かないため
（一度入れて外した）。

### 文字サイズは7段階だけ

`:root` の `--fs-xs`(12) / `--fs-sm`(14) / `--fs-base`(16) / `--fs-md`(19) / `--fs-lg`(22) /
`--fs-xl`(26) / `--fs-2xl`(32)、それに `--fs-mono`(14.5) のみを使う。
**基準（`--fs-base`）はヘッダーのナビと目次リンクのサイズ**で、本文・箱の中・ボタン・入力も同じ。

`font-size` を px で直接書かないこと（SVG の中だけは別系統）。`check.mjs` が検出して落とす。
狭い画面では個別の見出しを縮めるのではなく、`html{ --fs-2xl:… }` で**段階の定義そのものを差し替える**。

過去に「全体を少し大きく」で64か所を1.10倍した結果、13.5 / 14.5 / 15.5 / 16.5 / 18.5 …と
**20段階に増えて統一感が壊れた**。段階を増やす方向の変更はしない。

見た目の確認はブラウザで。**Claude が確認する場合は headless Chromium を使うこと**（Playwright）。
今日見つけた不具合の大半は、目視ではなく**測定**で見つかっている。

---

## 4. マークアップの規約

セクションはこの形。順序も守る。

```html
<section class="sec" id="一意なID">
  <header class="sec-head">
    <span class="sec-kicker">このドメインについて</span>  <!-- 意味のあるラベルがあるときだけ -->
    <h2>1-3　節の見出し</h2>                              <!-- 項番号は見出しの文字列に含める -->
  </header>
  <p class="task"><span class="tn">BLUEPRINT 1.1</span><b>公式タスク名</b></p>
  <h3 class="sub">小見出し</h3>        <!-- 複数トピックを含むときだけ -->
  <p class="lead">導入の1〜2文</p>
  <p class="goal"><b>ゴール</b>この節を終えたら何が言えるようになるか</p>
  <div class="figbox"><div class="diagram run"><svg viewBox="0 0 760 260">…</svg></div></div>
  <div class="detail">
    <h3>図の読み方</h3>
    <ol class="ann">
      <li><span class="n">1</span><span>…</span></li>
    </ol>
    <div class="ex"><span class="exl">見出し</span>本文</div>
    <div class="myth">
      <span class="t">誤解</span><span>…</span>
      <span class="t o">実際は</span><span>…</span>
    </div>
  </div>
  <p class="point"><b>ポイント</b>この節の結論を1〜2文</p>
  <p class="note">※ 補足・例外</p>
</section>
```

| クラス | 用途 |
|---|---|
| `.lead` | 節の導入。ここで「何の話か」を言い切る |
| `.goal` | **ゴール（到達基準）。** 「〜を説明できる」「〜を選び分けられる」の形で書く。`check.mjs` が有無を検査する |
| `.figbox` > `.diagram.run` > `svg` | 図。`.run` は画面に入ったときの段階表示 |
| `.ann` | 図の番号説明。`.figbox` の中に置くと「番号の説明」見出しが自動で付く |
| `.ex` / `.exl` | 具体例・対比 |
| `.myth` | 誤解 ⇄ 実際は。**試験で狙われる誤解**を書く場所 |
| `.point` | 節の結論。1〜2文に収める |
| `.note` | 補足・例外・細かい仕様 |
| `pre.code` + `.k .s .v .c .hl .rng` | JSON。`.k`=キー `.s`=文字列 `.v`=数値 `.c`=コメント `.hl`=強調 `.rng`=範囲 |
| `.task` | この節が対応する公式タスク。`<span class="tn">BLUEPRINT 1.1</span>` ＋ タスク名。対応しない節は「ブループリント対応なし ── …」と書く。`check.mjs` が有無を検査する |
| `.sec-kicker` | 意味のあるラベルがあるときだけ。「セクション」のような中身のない語は入れない（53個入れて全部外した） |

### 項を増やす・減らすとき

1. `docs/<ドメイン>/NN-<id>.html` を作り、`▼ 本文` 〜 `▲ 本文` の中に `<section class="sec" id="<id>">` を書く
2. `NN` は読む順。番号を詰めたいときは既存ファイルもリネームする
3. `<h2>` の先頭に項番号（`1-3　`）を入れる
4. `node tools/reindex.mjs` を実行する。**目次・送り・索引・カードは自動で付く**（手で足さない）

理解度チェックの節は `class="sec sec-quiz"` ＋ `data-quiz="キー"`、中に `<div class="qwrap"></div>` を置く。

---

## 5. 図の規約 — ここが一番事故る

### 色は必ず CSS 変数で書く

```html
<!-- ✗ テーマ切替で色が変わらない -->
<rect fill="#1B2740" stroke="#33456A"/>
<!-- ○ -->
<rect fill="var(--fig-fill-neutral)" stroke="var(--fig-line)"/>
```

使える変数（`assets/style.css` の `:root` と `[data-theme="light"]` に両テーマぶん定義済み）：

| 変数 | 意味 |
|---|---|
| `--fig-surface` | 図の中のカード地色 |
| `--fig-fill-neutral` / `--fig-line` | 中立のパネル・枠線 |
| `--fig-blue` / `--fig-fill-blue` / `--fig-line-blue` | **アプリ側**（送る側） |
| `--fig-amber` / `--fig-fill-amber` / `--fig-line-amber` | **モデル側**（返す側） |
| `--fig-green` / `--fig-fill-green` / `--fig-line-green` | **正しい・OK** |
| `--fig-red` / `--fig-fill-red` / `--fig-line-red` | **誤り・NG** |
| `--fig-muted` / `--fig-dash` / `--fig-bar` | 補助文字・破線・活性バー |
| `--fig-code-bg` | 図中のコード地色 |

色は**意味と結びついている**。青＝アプリが送る、橙＝モデルが返す、緑＝OK、赤＝NG。この対応を崩さない。

矢印は `marker-end="url(#ar|arb|ara|arr|arg)"`（灰・青・橙・赤・緑）。定義は各 HTML の先頭にある。

### 番号説明（`.ann`）の書き方

1. **位置ではなく、図に書いてある名前で指す。**
   ✗「左は…」「右上が…」 ○「**「例を1つ見せる」**（右・緑）── …」
   図にパネル名があるのに位置で指すと、読み手はどれを見ればいいか分からない。
2. **図に書いてあることを繰り返さない。** 番号説明には**図にない情報**を書く。
   「なぜそうなるか」「これがないと何が起きるか」「試験でどう問われるか」。
3. **図にない番号を振らない。** 番号は図の要素か、パネルの並び順に対応させる。

### 幅は 760 に固定、高さは自由

**`viewBox="0 0 760 <高さ>"` で作る。** 幅760は全64点で統一されていて、これが狭い画面の対応の前提になっている
（`style.css` の `@media (max-width:860px)` で、図を縮めずに `.figbox` の中だけ横スクロールさせている。
縮めると390px幅で文字が実効4.5pxまで潰れるため）。**幅を760以外にすると、その図だけ狭い画面で崩れる。**

図の縦幅に上限はない。**関連する内容は同じ節にまとめる**。「前の項の続き」で分けない。

**1ファイル＝1項＝1画面**。節を割って「続き」を作るのではなく、
**1項が1画面ぶんの読み物として完結している**こと。移動は目次・前後の送りで行う（ブラウザのページ遷移）。

---

## 6. 理解度チェック

設問は `assets/quiz-data.js` の `window.QUIZ` に、キーごとの配列で入っている。

```js
q4:[
 {q:"設問文",
  o:["選択肢A","正解の選択肢","選択肢C","選択肢D"], a:1,
  e:"なぜそれが正解か。誤答がなぜ誤りかも一言添える"},
]
```

- `a` は**0始まり**の正解インデックス
- HTML 側は `<section class="sec sec-quiz" data-quiz="q4">` ＋ `<div class="qwrap"></div>` だけ置けばよい。描画は `app.js` がやる

**設問はそのセクションまでに説明した範囲だけで解けること。**
先取りは学習の妨げになる。`node tools/check.mjs` が自動判定する。
誤答の選択肢に未説明の用語（`temperature` など）が出るのは問題ない — 正解を選ぶのに知識が要らないため。

---

## 7. これまでに直した問題 — 再発防止リスト

**すべて実際に起きた。同じことをしないこと。**

| # | 起きたこと | 教訓 |
|---|---|---|
| 1 | 「新人にマニュアルを渡す」「電話越しの専門家」というたとえが、**教えている仕組みと矛盾**していた（電話の相手は直前の話を覚えている＝「毎回まるごと送り直す」と正反対） | **たとえは図と1対1で対応するときだけ使う。**対応しないなら入れない。日常的な含意が仕組みと矛盾していないか、必ず確かめる |
| 2 | 番号説明が「左は…」「右は…」と位置で指していたが、図には**3つの名前つきパネル**があり、しかも②の説明は右ではなく**下のパネル**の内容だった | 位置ではなく**パネル名で指す**。`check.mjs` では拾えないので目視で確認 |
| 3 | 番号説明が図の文言を**そのまま繰り返して**いた | 番号説明には図にない情報を書く。`check.mjs` が18字以上の一致を検出 |
| 4 | CSS の `svg text{fill:…}` が SVG の `fill` 属性を上書きしていて、**全91ページで図の色分けが一度も効いていなかった**。SVG のプレゼンテーション属性は CSS より優先度が低い | 色は CSS 変数で書く（§5）。`check.mjs` が固定色の残留を検出 |
| 5 | 「プロンプトの基本」のテストに、**6ページ先の Task 4.3 の内容**を問う設問が入っていた。しかも後半のテストと重複 | 設問は既習範囲だけ。`check.mjs` が自動判定 |
| 6 | 図のテキストが枠から溢れていた（3か所） | ブラウザで `getBBox()` を測って確認する。座標だけの静的チェックでは足りない |
| 7 | 「もっと詳しく」を `<details>` で折りたたんでいた | **学習に有用な情報は折りたたまない。**情報量が多いならセクションを分ける |
| 8 | ページ統合後も「前ページ」「次ページ」という参照が14か所残っていた | 構造を変えたら位置参照を全文検索する。`check.mjs` が検出 |
| 9 | 教材が試験範囲を網羅していると思い込んでいたが、`tool_choice` `plan mode` `custom_id` `is_error` `.mcp.json` などが**0回も登場していなかった** | 「網羅しているか」は主観で答えず、**用語を grep して数える** |
| 10 | 「全体を少し大きく」の指示に対し64か所を一律1.10倍した結果、13.5 / 14.5 / 15.5 / 16.5 / 18.5 … と**段階が20種類に増えて統一感が壊れた** | 一律の倍率をかけない。**段階（トークン）を定義して、そこに割り当てる**。`check.mjs` が px 直書きを落とす |
| 11 | 図を幅に合わせて縮めていたため、390px 幅で**図中の文字が実効4.5pxまで潰れていた**（横溢れ0なので静的チェックでは無事に見えた） | 「崩れていない」＝「読める」ではない。**実効文字サイズを測る**。図は縮めず、箱の中だけ横スクロールさせる |
| 12 | 節番号（`1-3`）とブループリントのタスク番号（`1.1`）を並べて出していたが、**どちらがどちらか判別できなかった** | 番号は形（ドット／ハイフン）だけで区別させない。`BLUEPRINT 1.1` のように**出所を書く** |
| 13 | 挿入用スクリプトの成否を確かめずに「変更した」と報告し、実際には**何も起きていなかった。3回起きている** ── ①`:root` への段階定義の挿入（ガード条件に自分で弾かれた）②`check.mjs` のパス変更（シェルのクォートで置換文字列が壊れた）③比較表の CSS（挿入先に指定したコメントが存在せず `replace` が空振り。利用者に「デザインなしですか？」と指摘されて発覚） | **置換したら、その場で結果を grep して確かめる。** `console.log('ok')` を無条件に出さない。文字列置換は「一致しなければ exit 1」にする。CSS を足したら**ブラウザで `getComputedStyle` を測って効いていることを確認する** |
| 14 | 比較表に「ディレクトリ1つが1ドメイン」「1項＝1ファイル」と書いていた。**読み手にファイル構成は無関係。** さらに「数え方」の行が、公式側と教材側で別の話をしていて比較になっていなかった | 教材の文面に**実装の話（ファイル・ディレクトリ・HTML）を持ち込まない。** 表の行は、左右が**同じ観点について**述べていること |

---

## 8. 事実確認の原則

**API の仕様は必ず公式ドキュメントで裏取りする。** 記憶で書かない。

今日この方針で、思い込みを2回訂正している。

- 「`messages` に `role:"system"` は入れられない」→ **入れられる**（新しめのモデルでは、先頭以外に限り、会話途中の system 指示として）
- 「コンテキストウィンドウはプランで決まる」→ **モデルで決まる**（プランは使用量の話）

参照先：

- https://platform.claude.com/docs/en/build-with-claude/working-with-messages
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://platform.claude.com/docs/en/build-with-claude/extended-thinking
- https://platform.claude.com/docs/en/build-with-claude/streaming
- https://code.claude.com/docs/en/mcp
- https://support.claude.com/en/articles/8606394-how-large-is-the-context-window-on-paid-claude-plans

モデル世代で変わる具体的な数値（200K / 1M など）は**本文に書かない**。
問われるのは「窓は共有資源で、入力と出力が同じ枠を食い合う」という構造のほう。

---

## 9. git の現状

`.git` は Windows 側で作り直し済みで、**正常に動く**。ブランチ `main`、remote は
`https://github.com/xeexi/ccaf-learn.git`。以前ここに「壊れかけている」と書いてあったが、それは誤りだった
（実際の原因は下の lock ファイル）。

### セッションから commit するときの手順

マウント越しの git には落とし穴が2つある。**どちらも実際に踏んでいる。**

**1. `.git/*.lock` の残骸。** セッションが途中で切れると `.git/index.lock` が0バイトで残り、
以後の `git add` / `commit` がすべて弾かれる。しかもマウント越しの `rm` は既定で
`Operation not permitted` になる。**`mcp__cowork__allow_cowork_file_delete` で削除許可を取ってから消す。**
「消せません」と報告して終わらせない。

```bash
rm -f .git/index.lock          # ← 許可を取ってから
```

**2. `user.name` / `user.email` が未設定。** サンドボックスに global の gitconfig がないため、
`git config user.name` は空を返して **exit 1**。`&&` で繋いだコマンド列がここで黙って止まる。
コミット前に設定する：

```bash
git config user.name xeexi
git config user.email ki4shi.ito@gmail.com
```

### push はセッションからはできない ── Windows 側で叩く

コミットまではセッションで作れる（実フォルダの `.git` にそのまま入る）。**push だけが通らない。**
サンドボックスに GitHub の認証情報が一切ない ── `credential.helper` 未設定、`gh` なし、proxy 環境変数なし。
出るエラーは `fatal: could not read Username for 'https://github.com'`。
以前は git proxy が 403 を返していた（`... is not in this session's authorized repository set`）。
**失敗の仕方は変わるが、結論は同じ。**

```
cd C:\Users\guard\Documents\Claude作業用\ccaf-learn
git push origin main
```

設定 → Cowork の「クラウドで新しいタスクを実行する」をオフにすると、以後の**新規**タスクが手元で動く。
**動いているセッションは移動できない。**

### GitHub Pages で公開する

成果物を `docs/` に置いてあるのは、**Pages がブランチ公開で選べるフォルダが「リポジトリ直下」と
「`/docs`」の2つだけ**だから（公式ドキュメントで確認済み。`src` のような任意のフォルダは選べない）。
リポジトリの Settings → Pages で「Deploy from a branch」＋ `/docs` を選べばそのまま公開できる。
`docs/.nojekyll` は Jekyll を通させないための空ファイル。

注意：**Pages のサイトはリポジトリが private でも公開される。** また private リポジトリの Pages は有料プラン限定。

---

## 10. 引き継ぎ ─ 前のセッションが終わった時点の状態

**このリポジトリは 2026-08-08 に大きく組み替えた。** 以下は完了済みで、`docs/` に反映されている。

- **`docs/` へ集約**し、**1ファイル＝1項＝1画面**の88ファイル構成にした（旧：1ドメイン1ファイル＋JSで1節ずつ表示）
- **共通部を `reindex.mjs` のテンプレート1か所に集約**（§2）。節ファイルには本文だけが入る
- **文字サイズを7段階に整理**（§3）。基準はヘッダー・目次のサイズ
- **項番号を `<h2>` の文字列に入れ**、ブループリントのタスクは `BLUEPRINT 1.1` と表示（§1）
- 中身のない「セクション」ラベルを53個削除
- **トップページを作り直した** ── 表題は「CCAR-F 学習ノート」、説明文は削除し、
  代わりに「公式ブループリントと、この教材の対応」（比較表＋青ラベルの読み方）を置いた。
  カードと分量の数字は生成物
- **狭い画面に対応**（§5・§7 #11）。390〜1024px で横溢れ0、図中の文字が11px未満のページ0

### git の状態（2026-08-08 時点）

ブランチ `main`。**`f53de49`（組み替え前）までが push 済み**で、それ以降 ── `d022995`（docs/ へ集約・
88ファイル構成）以降のコミットが**未 push**。正確な差分は `git log --oneline origin/main..main` で見ること。

セッションからは push できないので、**Windows 側で `git push origin main`**（§9）。

検証は Playwright で全89ページ実施済み（節1つ・ナビ・目次・パンくず・送り・リンク切れ・
図のはみ出し・横スクロール・console エラー）。`node tools/check.mjs` も通っている。

---

## 11. 未着手・今後

- **図を根本から作り直す作業が途中。** 利用者の指摘は「既存の図を評価・改善するのではなく、
  各項目のゴールを達成するために最適な図を根本から考え直して」。測定した診断結果：
  図の中の文字数が中央値185字・最大470字、`.ann`（番号説明）が63点すべてに付いている、
  矢印のない「パネル並べ」が19点。**図の中は合計60字以内・箱はラベルのみ（1箱10字目安）**を
  基準にして、ゴールの型（区別／手順／対応／量）から形を決める方針まで合意済み。着手前
- **設問数がドメインの比率と釣り合っていない。** Domain 2（18%）が9問、Domain 3（20%）が20問
- ライトテーマは実機での確認が浅い。ダークで作ってから変数で対応させたため
- 公式タスク名の英語原文を併記する案（未着手。日本語訳のみ）

---

## 12. 利用者について（作業の進め方）

- **黙って進めない。**着手前に「やること」を本文の箇条書きで示し、長い作業では区切りごとに報告する
- **タスクリストのウィジェットは相手の画面に表示されていない。**進捗は必ず本文テキストで書く
- **判断が分かれた作業では**、迷った点・選ばなかった案とその理由を短く添える（毎回の実況は不要）
- **頼まれていない副産物を作らない。**メモや md を勝手に増やさない
- 指摘は的確。「この図はどれのこと？」「この設問は範囲外では？」といった問いは、**たいてい当たっている。**
  反射的に弁明せず、まず測って確かめること
