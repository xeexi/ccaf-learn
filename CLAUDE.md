# CCAR-F 学習ノート — 作業ガイド

このリポジトリを触る Claude 向けの引き継ぎ。**編集を始める前にこのファイルを最後まで読むこと。**
過去に実際に起きた失敗と、その再発防止策が「これまでに直した問題」に列挙してある。

---

## 1. これは何か

**CCAR-F（Claude Certified Architect – Foundations）合格**のための学習サイト。
利用者は日本語話者ひとり。**表記はすべて日本語**（コード・API のフィールド名を除く）。

出題ブループリント（5ドメイン・30タスク）をそのまま目次にしている。
「読み物として面白いか」より **「試験に出る内容を正しく理解できるか」を優先**する。
これは利用者が明示した方針。さらにその上に **「簡潔な説明で、できるだけ短時間で理解できること」**
が最上位の方針として乗る（2026-08-09 に明示された）。**図を増やすことも、説明を足すことも、
それ自体は目的ではない。**

本試験は **60問・120分**、合格は1000点満点中720点。比率で按分した問数は下表のとおり。

| ドメイン | 比率 | 問数 | ディレクトリ |
|---|---|---|---|
| 前提（全ドメイン共通の土台） | ─ | ─ | `docs/00-basics/` |
| Domain 1 エージェント設計とオーケストレーション | 27% | 約16問 | `docs/01-agentic/` |
| Domain 2 ツール設計と MCP 連携 | 18% | 約11問 | `docs/02-tools/` |
| Domain 3 Claude Code の設定とワークフロー | 20% | 約12問 | `docs/03-claude-code/` |
| Domain 4 プロンプト設計と構造化出力 | 20% | 約12問 | `docs/04-prompt/` |
| Domain 5 コンテキスト管理と信頼性 | 15% | 約9問 | `docs/05-context/` |
| まとめ・模擬試験 | ─ | ─ | `docs/06-summary/` |

比率と問数の出所は `tools/reindex.mjs` の `DOMAINS` と `EXAM_Q`。**HTML に直接書かない。**

### 番号が2系統ある ─ 混同しないこと

| | 形 | 意味 |
|---|---|---|
| **タスク番号** | `1.1`（ドット） | 公式ブループリントの出題単位。全30個。本文では `BLUEPRINT 1.1` と表示する |
| **項番号** | `1-3`（ハイフン） | この教材の読む単位。ドメイン内の通し番号。`<h2>` の先頭に入れる |

1タスクを複数の項に割ることがある。逆に、どのタスクにも対応しない項（前提・導入・まとめ・模擬）もある。
**対応がない項にはラベルを出さない**（「ブループリント対応なし」とは書かない ── 読み手に情報がないため）。
この対応関係は `docs/index.html` の「2種類の番号」で読み手に説明済み。

**ドメインをまたぐラベルが2件ある。** 教材の読む順と公式の出題単位がずれている箇所で、
どちらもラベルの後ろに「なぜここにあるか」を書いてある。増やすときも同じようにすること。

| 節 | 節のドメイン | ラベル |
|---|---|---|
| 1-2 ツールを渡すとは | Domain 1 | `2.1`（ループを読む前に必要なため） |
| 5-4 確実にしたいことは仕組み側へ | Domain 5 | `1.5`（長く動かす話として扱うため） |

採点思想は **「効く一番単純な手」**。設問で凝った選択肢はたいてい誤答。教材全体がこの基準で書かれている。

---

## 2. ファイル構成

```
docs/                   ← 成果物。この中だけで完結して動く（GitHub Pages の /docs にそのまま出せる）
  index.html            トップ（配点・読む順番・2種類の番号・全項目一覧）
  00-basics/            ドメインごとのディレクトリ
    01-flow.html          1ファイル＝1項。ファイル名は NN-<節のid>.html（NN は読む順）
    02-resend.html
    …
  01-agentic/ … 06-summary/
  assets/
    style.css           全スタイル。ダーク（既定）／ライトの2テーマ
    app.js              テーマ切替・ナビ／目次の開閉・クイズ描画・操作できる図・全文検索
                        キー操作（/ Esc ← →）は**1つの keydown にまとめる**（5t が見る）
    quiz-data.js        全設問データ（window.QUIZ）
    search-index.js     全文検索インデックス（自動生成物・手で編集しない）
  .nojekyll             GitHub Pages に Jekyll を通させないための空ファイル
tools/
  x                     docker ラッパー。**これ経由で回す**（下の §3）
  blueprint.mjs         **公式 Exam Guide 由来の事実は、すべてここが出所**（§8）
  reindex.mjs           節ファイルの周りを書き出す ＋ 共通の塊を差し込む ＋ 索引 ＋ ?v=
  check.mjs             静的チェック（過去の不具合の再発検出）
  measure.mjs           ブラウザ実測（文字サイズ・右端・横溢れ・折り返し・送り・ナビ・コントラスト）
  .cache/               playwright と測定結果。**gitignore 済み**
CLAUDE.md / README.md
```

全95項（本文73・理解度チェックだけの項22）。模擬シナリオ7項は本文と設問が同じ項にある。外部依存は **Google Fonts のみ**（Inter / Noto Sans JP / JetBrains Mono）。
オフラインでもシステムフォントにフォールバックして読める。ビルドもサーバも不要で、`docs/index.html` を開けば動く。

### 節ファイルの中身は「本文」だけ

head・上部ナビ・矢印defs・目次・パンくず・前後の送り・検索窓は、
`tools/reindex.mjs` のテンプレートが**唯一の出所**で、実行するたびに90ファイルへ書き出される。

```html
<!-- ▼ 本文 ─ ここだけを編集する。この前後は node tools/reindex.mjs が書き出す -->
<section class="sec" id="loop">…</section>
<!-- ▲ 本文 -->
```

**この目印の外を手で編集しても、次の `reindex` で消える。** 共通部を変えたいときは `reindex.mjs` を直す。

### 2か所以上に出るものは `<!--#名前-->` で差し込む

同じ図や表が複数ページに要るときは、本文に目印だけ置いて `reindex.mjs` の `BLOCKS` に実体を書く。
手で2か所コピーすると、必ず片方だけ古くなる。

```html
<!--#weightfig--><!--/#weightfig-->     <!-- 配点の図。トップと 6-2 の2か所で使っている -->
```

`docs/index.html` の全項目一覧も `<!--#alllist-->` の中に生成される。手で書かない。

**出題タスクの名前も同じ扱い。** 本文には番号だけ書く：

```html
<p class="task" data-t="1.1"></p>        <!-- 2つ持つ節は data-t="5.2 5.5" -->
```

`reindex.mjs` の `TASK_NAMES`（公式 Exam Guide の原文30件）が唯一の出所。
**訳さない** ── 設問は英語で書かれるので、原文のまま覚えるほうが結びつく。
同じ番号が複数の節に出る（1.1 は5節、2.1 は3節）ので、手で書くと必ず片方だけ古くなる。

---

## 3. 編集の原則

**正典は各節ファイルの「本文」部分そのもの。** それ以外は生成物。

### この環境には node が入っていない ── docker で回す

WSL 側に node はない。`tools/x` が docker 越しに実行する。**`node tools/…` と直に打たない。**

検査は必ず**対象が何件あったか**を出す（`✓ 注記 40 件に丸写しなし`）。
0件のまま `✓` が出ると、通ったのか見ていないのかが区別できない（§7 #37）。

```bash
./tools/x reindex   # 本文の周りを書き出す ＋ 索引 ＋ ?v=
./tools/x check     # 静的チェック（exit 1 なら要修正）
./tools/x measure   # ブラウザ実測（代表11ページ。--all で全91ページ）
./tools/x node …    # 任意の node コマンド（設問数を数えるときなど）
./tools/x shell     # コンテナの中に入る
```

編集したら **必ず `reindex` → `check` の順で**。`reindex` を忘れると検索が本文とズレる。

`tools/x` の要点：

- **`-u $(id -u):$(id -g)` を必ず付ける。** 付けないと reindex が書き出す HTML が root 所有になり、
  以後ホスト側から編集できなくなる（ラッパーに入れてある）
- Playwright 公式イメージは**ブラウザだけ**同梱していて、ライブラリは入っていない。
  `tools/.cache` に `playwright@<イメージのタグと同じ版>` を入れる。**版がズレるとリビジョン検査で落ちる**
- ESM の `import` は `NODE_PATH` を見ない。`measure.mjs` は `createRequire` で `tools/.cache` から読んでいる
- **その場かぎりのブラウザ計測は `CCARF_NODE_IMAGE` でイメージを差し替える。**
  `./tools/x node` は node イメージなのでブラウザが無い ── `measure` 以外で Playwright を使うときはこう書く：

  ```bash
  CCARF_NODE_IMAGE=mcr.microsoft.com/playwright:v1.62.1-noble ./tools/x node tools/.cache/shot.mjs
  ```

  スクリプト側は `createRequire('/w/tools/measure.mjs')` で `playwright` を読む。
  **画素を見たいときは、要素を高倍率で撮って canvas に戻す** ── 丸の中の数字の位置はこれで測った（§7 #80）

`reindex.mjs` がすること：

1. 各節ファイルの本文（`▼ 本文` 〜 `▲ 本文`）を読み取る
2. `<!--#名前-->` の共通の塊を差し込む
3. その前後（head・上部ナビ・矢印defs・目次・パンくず・前後の送り・検索窓）を書き出す
4. `assets/search-index.js` を本文から作り直す
5. `assets/*.css|js` 参照に `?v=<中身の SHA1 先頭8桁>` を打ち直す

5 はキャッシュ対策。これがないと、ブラウザが古い `search-index.js` や `style.css` を使い続けて、
本文と検索結果がズレる。**`<meta http-equiv="Cache-Control">` は使わない** —
ブラウザが確実に尊重せず、そもそも CSS / JS のような下位リソースには効かないため（一度入れて外した）。

**HTML を書き換えるので、実行後の差分に HTML が含まれるのは正常。**

### 文字サイズは5段階だけ

`:root` の5つのみを使う。**これ以外を増やさない。**

| 段階 | px | 用途 |
|---|---|---|
| `--fs-xs` | 12 | ラベル・チップ・キャプション・図の中の注記 |
| `--fs-sm` | 14 | 補助テキスト・注記・小項目・コード・**図の中の見出しと本文** |
| `--fs-base` | 16 | **基準。** ヘッダーのナビ・目次リンク・本文・ボタン・入力 |
| `--fs-lg` | 26 | セクション見出し（`.sec-head h2` / `.hero h2`）と**節の中の小見出し（`h3.sub`）** |
| `--fs-xl` | 32 | ページ見出し（`.hero h1`） |

**基準はヘッダーのナビと目次リンクのサイズ。本文もそれと同じ。**
図を HTML で組むようになったので、**図の中の文字もこの段階に乗る**（SVG のときは別系統だった）。

段階の外の値を作る書き方が3つある。**すべて `check.mjs` が落とす。**

1. `font-size` の px 直書き（残っている SVG の中だけは別系統）
2. `clamp()` ── 幅に応じた中間サイズを無限に作る
3. `em` / `rem` / `%` ── 親のサイズぶんだけ段階外の値ができる

狭い画面では個別の見出しを縮めるのではなく、`html{ --fs-xl:… }` で**段階の定義そのものを差し替える**。

### 読み幅は 1000px にそろえる

節ページの本文幅は `.shell` の 1360 − 余白52 − 目次264 − 隙間44 で **1000px**。
トップページには目次がないので、`.hero` に `max-width:1052px`（1000＋余白26×2）を当てて同じ列幅にしている。

**個々の要素に `max-width` を当てて狭めないこと。** 幅は容器側で1回だけ決める。

### 見た目はブラウザで測る ── `./tools/x measure`

**目視ではなく測定で直す。** 見つかった不具合の大半は測定で発覚している。

```bash
./tools/x measure --engine=webkit --all --only=overflow,contrast --widths=390,1440
```

| 引数 | 意味 |
|---|---|
| `--engine=` | `chromium`（既定）／ `webkit`。**iPhone の検証は必ず webkit**。日本語の行分割と `text-wrap` の実装が違う |
| `--all` | 全91ページ（既定は代表11ページ） |
| `--widths=` | 390 / 640 / 860 / 1024 / 1440（既定） |
| `--themes=` | dark / light |
| `--only=` | `size` `right` `overflow` `wrap` `pager` `nav` `contrast` |
| `--css=` | CSS を注入して A/B 比較する（`--css='.lead{text-wrap:wrap}'`） |

コントラストを測るときの注意が4つある。**4つとも実際に誤った結果を出した。**

1. `color-mix()` や `color()` は **0〜1 の小数**で返る。0〜255 と決めつけない。
   **canvas に描いて `getImageData` で読む**
2. 半透明の地色は**祖先をたどって合成する**。1つ見つけて止めると値が狂う
3. **自分の背景を持つ要素**（番号の丸・帯の区画）は、その要素自身の背景の上に文字が乗る。
   親から測ると全部 1:1 になる
4. `transition` が効いていると**遷移中の中間色**を拾う。`*{transition:none !important}` を注入してから切り替える

SVG の中の文字は、`.figbox` の地で近似すると色つきの箱に乗った文字が全部誤判定になる。
`elementsFromPoint` で**真下の図形**を拾う（`measure.mjs` はそうしている）。

**スクリーンショットを撮るときは 1.6 秒待つ。** 図は画面に入ると段階表示（`.run`）が再生し直されるので、
`scrollIntoViewIfNeeded()` の直後に撮ると**真っ白な図が写る**（実際に1回引っかかった）。

---

## 4. マークアップの規約

セクションはこの形。順序も守る。

```html
<section class="sec" id="一意なID">
  <header class="sec-head">
    <h2>1-3　節の見出し</h2>                              <!-- 項番号は見出しの文字列に含める -->
  </header>
  <p class="goal"><b>ゴール</b>この節を終えたら何が言えるようになるか</p>  <!-- 見出しの直下。5c が位置を検査する -->
  <p class="lead">導入の1〜2文</p>
  <p class="task"><span class="tn">BLUEPRINT 1.1</span><b>公式タスク名</b></p>

  <div class="figbox"><figure class="fig run">…</figure></div>   <!-- 図（§5） -->
  <table class="tbl">…</table>                                    <!-- または比較表（§5） -->

  <p class="point"><b>ポイント</b>この節の結論を1〜2文</p>

  <div class="detail">
    <div class="ex"><span class="exl">見出し</span>本文</div>
    <div class="myth">
      <span class="t">誤解</span><span>…</span>
      <span class="t o">実際は</span><span>…</span>
    </div>
  </div>
  <p class="note">※ 補足・例外</p>
</section>
```

### 面（背景＋枠）を持つのは「図」と「ポイント」だけ

**箱を増やさない。** ライトテーマで白い箱が7つ縦に並び、どれが結論か分からない状態になったことがある（§7 #21）。

| 強さ | 要素 | 見た目 |
|---|---|---|
| **1** | `.point` | **アクセント色を地に敷いた面。ページで唯一いちばん強い** |
| 2 | `.figbox` | 面＋枠。図の容器 |
| 3 | `.ex` | 左の**アクセント罫**＋アクセントのラベル |
| 3 | `.myth` | 左の赤罫 |
| 4 | `.note` | 左の薄い罫のみ。いちばん弱い |
| ─ | `.goal` | **罫も面も持たない、見出し直下の1行**（§7 #75） |

**グレーの左罫（`--line` / `--line-soft`）は使わない。** 地との対比が
dark 1.52 / light 1.21 で、**罫があること自体が見えない**（実測）。区画を示すなら
アクセントか赤。示さないなら罫を置かない。

| クラス | 用途 |
|---|---|
| `.lead` | 節の導入。**第1文は、その節を知らない人に通じる1文**にする ── 専門用語・前方参照（「この3つ」）・位置参照（「前節の」）で始めない。他の節を指すときは**節の名前でリンク**する（`〈<a href="…">節名</a>〉`）|
| `.goal` | **ゴール（到達基準）。** 「〜を説明できる」「〜を選び分けられる」の形。**見出しのすぐ下に置く。節の頭だけでなく、小見出し（`h3.sub`）にも必ず置く** ── 新しい知識を教える区画には、その説明がいちばん短い手かを測る基準が要る（`check.mjs` 5c が有無と位置を検査する） |
| `.task` | 対応する公式タスク。`<span class="tn">BLUEPRINT 1.1</span>` ＋ タスク名。**対応がないなら書かない** |
| `.figbox` > `figure.fig.run` | 図（§5）。`.run` は画面に入ったときの段階表示 |
| `.tbl` | 比較表。`<td data-l="列見出し">` を付ける ── 狭い画面ではそれを見出しに1行＝1カードへ畳む |
| `.ex` / `.exl` | 具体例・対比。**ラベル（`.exl`）の中で `<b>` を使わない** ── ラベルは色そのものが役割なので、中で色が変わると別種のラベルに見える。強めたいときは「」で囲う（`check.mjs` 5r が検査する） |
| `.myth` | 誤解 ⇄ 実際は。**試験で狙われる誤解**を書く場所 |
| `.point` | 節の結論。1〜2文に収める。**同じ節に同じ文言を2回置かない**（14件あって削除した） |
| `.note` | 補足・例外・細かい仕様 |
| `pre.code` + `.k .s .v .c .hl .rng` | JSON **と YAML**。`.k`=キー `.s`=文字列 `.v`=数値 `.c`=コメント `.hl`=強調 `.rng`=範囲。**YAML を出すときは「YAML frontmatter」と明記する** ── 色分けが同じなので、書かないと JSON に見える（§7 #34） |
| `.ann` | **原則廃止。** 残っているのは `pre.code` の中の `<span class="n">1</span>` と対応させる注記だけ |

`.sec-kicker`（見出しの上の小さいラベル）は、**まとめの模擬試験だけに残っている**（6-4 と7つのシナリオ、すべて「模擬試験」の8か所）。
**7つのシナリオは `<h2>` の題を丸番号で始める**（`6-5　① 社内の…`）── 目次・タブ・全項目一覧・検索は `<h2>` から作られるので、そこに入れないと**そのページを開いたときしか見えない**（ラベルに「シナリオ ①」と書いていたときがそうだった）。
中身のないラベルを53個入れて全部外した経緯があるので、増やさない。
**`<h2>` と同じ意味のラベルを重ねない** ── 設問ページに「理解度チェック」「シナリオ ① の設問」という
ラベルが付いていて、見出しと二重になっていた（8件削除した）。

### 項を増やす・減らすとき

1. `docs/<ドメイン>/NN-<id>.html` を作り、`▼ 本文` 〜 `▲ 本文` の中に `<section class="sec" id="<id>">` を書く
2. `NN` は読む順。番号を詰めたいときは既存ファイルもリネームする
3. `<h2>` の先頭に項番号（`1-3　`）を入れる
4. `./tools/x reindex` を実行する。**目次・送り・索引・全項目一覧は自動で付く**（手で足さない）

### 設問の置き方は2通り

| | 形 | 使う場面 |
|---|---|---|
| **設問だけの項** | `class="sec sec-quiz"` ＋ `data-quiz="キー"` | 直前の複数の項をまとめて確かめる（`1-9 理解度チェック ─ 往復の中身と壊れ方` など）。**見出しは必ず「理解度チェック ─ 副題」** |
| **本文と同じ項に置く** | 本文の `<section class="sec" id="…" data-quiz="キー">` ＋ 末尾に `<h3 class="sub">では、どう手を打つか</h3>` | **設問がその項の文脈に依存する**とき。模擬シナリオ7本がこれ ── 状況を読みながら答えるので、別ページにすると状況が画面外になる（§7 #49） |

**本文と同じ項に置くほうは「理解度チェック」と名乗らせない。** ページ見出しが
「6-5 社内の問い合わせに答えるエージェント」なのに小見出しが「理解度チェック ─ 社内の問い合わせ」だと
**副題が丸かぶり**で、しかも「理解度チェック」は**独立した確認の項**の名前として使っている（§7 #51）。

どちらも中に `<div class="qwrap"></div>` を置く。`app.js` は `[data-quiz]` を見るので `sec-quiz` は要らない。
「総合チェック」「解いてみる」のような別名を作らない ── 3通りの名前が混在して一覧で区別できなくなった。
`check.mjs` の見出し検査は**設問だけの項**（`quizOnly`）にだけ効く。

**副題は必ず付ける**（`─ ループ` `─ セッション`）。数に関係なく全件。
以前は「その区分に1つだけなら不要」としていたが、25件中2件だけ副題なしになり、
一覧で文が途切れて見えた（§7 #36）。
副題は**内容語で書く。** 「タスク 3.1」のような参照は、読み手に番号を引き直させるので使わない。
ドメイン全体を扱うものは `─ Domain 3 総合` `─ 全ドメイン総合` の形もある。

---

## 5. 図の規約 — ここが一番事故る

### 図は HTML で組む。SVG は「形そのものが情報」のときだけ

**幅760固定の SVG はやめた。** iPhone では箱の中を横スワイプすることになり、一度に見渡せない。
HTML で組めば **PC は横並び・iPhone は縦1列**に折り返り、図の中の文字も実テキストなので
どの幅でも 14px のまま読める（390px で実効4.5pxまで潰れる問題が構造的に消える）。

```html
<div class="figbox"><figure class="fig run">
  <p class="fig-t">図のタイトル ─ 何を見る図か</p>
  <div class="fig-row" style="--cols:2">
    <div class="fig-card s1"><b class="fig-h">ラベル</b>
      <span class="fig-note">一言（16字以内）</span></div>
    …
  </div>
  <p class="fig-f">この図で言いたいこと（1行）</p>
</figure></div>
```

**`<figure>` の UA 既定は `margin:1em 40px`。** `.fig{ margin:0 }` を外すと左右で80pxはみ出す（実際に踏んだ）。
**`<span>` に `width` は効かない。** 帯を描くときは `display:block` を付ける（これも踏んだ）。

| 部品 | 用途 |
|---|---|
| `.fig-t` / `.fig-f` | 図のタイトル／結び。**説明は図の中に置く**（§「説明は図の中へ」） |
| `.fig-lbl` | 図の中の小見出し。段が何を表すか |
| `.fig-row` ＋ `--cols:N` / `.fig-card` | 箱を並べる。iPhone では1列に折り返す |
| `.fig-row.flow` | 箱の間に矢印。横並びなら `→`、縦積みなら `↓` に変わる |
| `.fig-steps` / `.fig-who` / `.fig-do` | 手順。番号の丸＋「誰が」＋内容。分担がそのまま読める |
| `.fig-seq`（§下） | シーケンス図。やり取りの向きを見せる |
| `.fig-bars` / `.fig-bar` | 量・比率の帯 |
| `.fig-seg` / `.fig-legend` | 1本を分け合う積み上げ帯＋色つきの凡例 |
| `.fig-turn` / `.fig-msgs` | 回を追うごとに増えるメッセージ列 |

### シーケンス図 ─ やり取りは原則これ

「誰から誰へ」を箇条書きで書くと向きを読み取るのに一拍かかる。矢印なら**向きが形そのもの**になる。
利用者から「シーケンス図は good」と評価されている形。

```html
<div class="fig-seq lanes3">                <!-- レーンは2本が既定。3者なら .lanes3 -->
  <div class="sq-head">
    <span class="sq-c">フック</span><span class="sq-b">Claude Code</span><span class="sq-a">Claude</span>
  </div>
  <div class="sq-body">                      <!-- ライフラインはここの背景として引く -->
    <div class="sq-row cb k-amber"><span class="sq-lbl"><span class="sq-n">1</span>…</span>
      <span class="sq-note">…</span></div>
    <div class="sq-self mid"><b><span class="sq-n">4</span>…</b><ul>…</ul></div>
  </div>
</div>
```

- **ライフラインは `.sq-body` の背景**。見出しが2行になっても位置がずれない
- 幾何（`to-r` `to-l` / `ab` `ba` `bc` `cb`）と色（`k-blue` `k-amber`）は**別**。
  3レーンでは「右向き＝送る」が成り立たないため
- `.sq-self` は自分の中だけで完結する処理、`.sq-idle` は相手が動いていない区間
- **番号はすべて `.sq-n`（丸付き）にそろえる。** 矢印の行だけ丸付きで箱の中は素の数字、という不統一をやった
- `.sq-self > b` のようにセレクタを直下に限定する。子孫の `<b>` まで `display:block` にすると行が崩れる

### 色は意味と結びついているときだけ使う

| 変数 | 意味 |
|---|---|
| `--fig-blue` / `--fig-fill-blue` / `--fig-line-blue` | **アプリ側**（送る側） |
| `--fig-amber` / `--fig-fill-amber` / `--fig-line-amber` | **モデル側**（返す側） |
| `--fig-green` / `--fig-fill-green` / `--fig-line-green` | **正しい・推奨** |
| `--fig-red` / `--fig-fill-red` / `--fig-line-red` | **誤り・危険** |
| `--fig-fill-neutral` / `--fig-line` | 中立 |
| `--fig-surface` / `--fig-muted` / `--fig-dash` / `--fig-code-bg` | 図の地・補助文字・破線・コード地 |

**「区別したいだけ」で色を使わない。** 対等な4項目に4色を当てて、赤＝危険・緑＝OK の対応を壊したことがある。
**「ここが要点」と示したいだけなら `.fig-card.key`**（アクセントの縁）を使う。

`--fig-line-*` は**線の色**。地色に使うと文字が読めない（実測 4.25:1 / 1.98:1 で AA 未達）。

残っている SVG では、色は必ず `var(--fig-*)` で書く（固定色は `check.mjs` が落とす）。
矢印は `marker-end="url(#ar|arb|ara|arr|arg)"`。

### 説明は図の中へ ─ `.ann` は原則廃止

図の外に番号説明（`.ann`）を置くと、読み手は図と本文を往復することになる。**短時間で理解する**という
最上位の方針に反するので、説明は図の中（`.fig-note` / `.fig-f`）に書く。

どうしても外に出すときは、**図の中の印と本文側の印を色や記号で対応させる**。
番号だけで外に出すのは禁止（位置で指して「どのパネルの話か分からない」事故を起こしている）。

図の中の文字の長さ。**目安は実測から決めた**（全90項の分布）。

| | 中央 | 75% | 90% | 目安 | `check.mjs` が落とす |
|---|---|---|---|---|---|
| `.fig-h`（箱ラベル） | 7字 | 11字 | 16字 | **10字** | **24字超** |
| `.fig-note`（注記） | 17字 | 25字 | 37字 | **25字** | **48字超** |
| `.sq-note`（矢印の注記） | 19字 | 34字 | 41字 | 35字 | 60字超 |

**以前「注記16字」と書いていたが、実測の中央値は17字で、目安が現実と合っていなかった**（§7 #38）。
上限は「明確に文が入った」ものだけを捕まえる高さに置いてある ── 目安を超えたら直す、上限を超えたら落ちる。

**`・A ・B ・C` を1つの注記に詰めない。** `.fig-card ul` があるのでリストにする。
長さの問題ではなく、**構造の問題**（48字を超えた5件のうち3件がこれだった）。

API の値のように短くできない識別子（`model_context_window_exceeded` など）は、
ラベルを `<code>` だけで構成すれば上限の対象外になる。

### 図にしない判断も結論のうち

ゴールの形から見せ方を決める。**本文73節のうち、図を置くのはおよそ半分。**
残りは比較表・コード・テキスト例にするか、何も置かない。節ごとの判定は §10 にある。

| ゴールの型 | 例 | 結論 |
|---|---|---|
| **手順・順番**（時間で進む） | 1-3 ループ、1-6 誰が何を送るか | **図**（シーケンス図・`.fig-steps`） |
| **対応・階層** | 3-2 CLAUDE.md の階層 | **図**（入れ子・対応表） |
| **量・比率** | 0-3 窓、配点 | **図**（`.fig-bars` / `.fig-seg`） |
| **区別**（AとBを選び分ける） | 1-8 壊れ方、1-12 チェーン | **比較表 `.tbl`。** 図より速く、iPhone でも勝手に折り返す |
| **形そのもの**（JSON の構造） | 1-7 実物、4-5 thinking | **図にしない。** `pre.code` に注記 |
| **概念の定義**（1文で言える） | 各ドメインの導入 | **何も置かない。** `.lead` ＋ `.point` |

**1ファイル＝1項＝1画面**。節を割って「続き」を作るのではなく、
1項が1画面ぶんの読み物として完結していること。移動は目次・前後の送りで行う。

---

## 6. 理解度チェック

設問は `assets/quiz-data.js` の `window.QUIZ` に、キーごとの配列で入っている。

```js
q4:[
 {q:"設問文",
  o:["選択肢A","正解の選択肢","選択肢C","選択肢D"], a:1,
  e:"なぜそれが正解か。誤答がなぜ誤りかも一言添える"},
]
```

- `a` は**0始まり**の正解インデックス
- HTML 側は `<section class="sec sec-quiz" data-quiz="q4">` ＋ `<div class="qwrap"></div>` だけ置けばよい

**設問数を HTML に書かない。** `.lead` の先頭に空の `<span class="qcount"></span>` を置くと、
`app.js` がそこに `N問。` を入れる。手で書くと足したときに必ず片方だけ古くなる（§7 #45）。

```html
<p class="lead"><span class="qcount"></span>選択肢をクリックすると、その場で正誤と解説が出ます。</p>
```

節ごとに言うことがあるなら、`</span>` の後ろに書く（「状況 A・B・C にそれぞれ答えます。」）。
**`.lead` を丸ごと上書きする実装に戻さないこと** ── その一文が消える。

**選択肢は `app.js` が描画のたびに混ぜる**（Fisher–Yates。組み立て時の1回だけなので、
答えたあとに並びは動かない）。だから **`quiz-data.js` の並びは読み手に届かない** ──
正解を何番目に書いても構わない。

そのかわり、**並びに依存する書き方をしてはいけない。**
「上記のすべて」「前者／後者」「選択肢 B」などが1つでも混ざると、その設問は解けなくなる。
`check.mjs` の 5i が全問を走査して落とす（4択であること・重複がないこと・`a` が範囲内であることも同時に見る）。

**設問数を数えるときは正規表現で切り出さない。** `quiz-data.js` を実際に読み込んで数える。
キーの範囲を正規表現で切り出して数えたら **92問を350問と誤った**（キーの終わりを取り違えて
後続のキーまで飲み込んでいた）。`./tools/x node -e` で `global.window={}` を用意して読み込み、
各ページの `data-quiz="…"` からキーを引いて配列の長さを足すのが確実。

**設問はそのセクションまでに説明した範囲だけで解けること。**
先取りは学習の妨げになる。`./tools/x check` が自動判定する。
誤答の選択肢に未説明の用語（`temperature` など）が出るのは問題ない — 正解を選ぶのに知識が要らないため。

---

## 7. これまでに直した問題 — 再発防止リスト

**すべて実際に起きた。同じことをしないこと。**

**検査 ID（`5g` `5v ①` など）が添えてある項は、`./tools/x check` が代わりに見張っているので短く畳んである。**
**添えていない項は、文章でしか守られていない** ── 判断・作法・測り方は検査に変換できないため。畳むときに残すのは
① 具体的な失敗　② 検査にできない洞察（測り方・判断）　③ 検査 ID　の3つだけ。

| # | 起きたこと | 教訓 |
|---|---|---|
| 1 | 「新人にマニュアルを渡す」「電話越しの専門家」というたとえが、**教えている仕組みと矛盾**していた（電話の相手は直前の話を覚えている＝「毎回まるごと送り直す」と正反対） | **たとえは図と1対1で対応するときだけ使う。**日常的な含意が仕組みと矛盾していないか、必ず確かめる |
| 2 | 番号説明が「左は…」「右は…」と位置で指していたが、図には**3つの名前つきパネル**があり、②の説明は右ではなく**下のパネル**の内容だった | 位置で指さない。いまは `.ann` 自体を廃止し、説明を図の中に置く（§5） |
| 3 | 番号説明が図の文言を**そのまま繰り返して**いた | 図にない情報を書く。`check.mjs` が18字以上の一致を検出 |
| 4 | CSS の `svg text{fill:…}` が SVG の `fill` 属性を上書きしていて、**全91ページで図の色分けが一度も効いていなかった** | 色は CSS 変数で書く（§5）。`check.mjs` が固定色の残留を検出 |
| 5 | 「プロンプトの基本」のテストに、**6ページ先の Task 4.3 の内容**を問う設問が入っていた | 設問は既習範囲だけ。`check.mjs` が自動判定 |
| 6 | 図のテキストが枠から溢れていた（3か所） | ブラウザで測って確認する。座標だけの静的チェックでは足りない |
| 7 | 「もっと詳しく」を `<details>` で折りたたんでいた | **学習に有用な情報は折りたたまない。** 情報量が多いならセクションを分ける（5v ①） |
| 8 | ページ統合後も「前ページ」「次ページ」という参照が14か所残っていた | 構造を変えたら位置参照を全文検索する。`check.mjs` が検出 |
| 9 | 教材が試験範囲を網羅していると思い込んでいたが、`tool_choice` `plan mode` などが**0回も登場していなかった** | 「網羅しているか」は主観で答えず、**用語を grep して数える** |
| 10 | 「全体を少し大きく」の指示に対し64か所を一律1.10倍した結果、**段階が20種類に増えて統一感が壊れた** | 一律の倍率をかけない。**段階を定義して、そこに割り当てる** |
| 11 | 図を幅に合わせて縮めていたため、390px 幅で**図中の文字が実効4.5pxまで潰れていた**（横溢れ0なので静的チェックでは無事に見えた） | 「崩れていない」＝「読める」ではない。**いまは図を HTML で組んで折り返す**（§5） |
| 12 | 節番号（`1-3`）とタスク番号（`1.1`）を並べて出していたが、**どちらがどちらか判別できなかった** | 番号は形だけで区別させない。`BLUEPRINT 1.1` のように**出所を書く** |
| 13 | 挿入用スクリプトの成否を確かめずに「変更した」と報告し、実際には**何も起きていなかった。3回起きている** | **置換したら、その場で結果を grep して確かめる。** 文字列置換は「一致しなければ exit 1」にする |
| 14 | 比較表に「ディレクトリ1つが1ドメイン」と書いていた。**読み手にファイル構成は無関係** | 教材の文面に**実装の話を持ち込まない。** 表の行は左右が**同じ観点について**述べていること |
| 15 | 「段階は7つ」と書いてあったが、**実際に画面に出ていたのは7種類ではなかった** | トークンを grep するだけでは足りない。**画面に出ている実測値の種類**を数える |
| 16 | `.hero p` にだけ `max-width:48em` が付いていて、段落が**400px 手前で折り返していた** | **読み幅は容器側で1回だけ決める**（5v ②） |
| 17 | ライトテーマで**アクセント色だけ上書きし忘れて**いた。コントラスト 1.0〜1.8:1 が2000か所以上 | **テーマを足したら変数の集合を突き合わせる。** `color-mix` で作る地色は**両テーマで向きが逆になる** |
| 18 | 「`app.js` がテーマを復元していない」と報告したが**誤り**だった。復元は `reindex.mjs` の `THEMEBOOT` が担っていた | 1つのファイルを見ただけで「機能がない」と言わない。**動かして確かめる** |
| 19 | `text-wrap:pretty` を当てていたため、**iPhone（WebKit）で行末に4〜6文字ぶんの空きが出ていた**。Chromium では 10〜14px にしかならず、PC で見ても気づけない | **iPhone の検証は webkit で測る**（`--engine=webkit`）。Chromium と行分割の実装が違う（5v ③） |
| 20 | ボタンが押せるように見えなかった。原因は**ライトテーマで `--surface-2` が `#FFFFFF`**、つまり地の白と同色だったこと | 押せるものは面を独立させる（`--btn-face` / `--btn-line` / `--btn-raise`）。**テーマごとに実際の色を突き合わせる** |
| 21 | ライトテーマで**白い箱が7つ縦に並び**、`.point`（結論）が他と同じ強さだった | **面を持つのは「図」と「ポイント」だけ**（§4）。箱を増やす方向の変更はしない |
| 22 | 対等な4項目に**4色を全部当てて**、赤＝危険・緑＝OK の対応を壊した | **区別したいだけで色を使わない。** 強調は `.key`（§5） |
| 23 | ループの1手目を「モデルが読んで決める」から始めていた。**モデルは呼ばれて初めて動く**ので、同じページの結び（「動かしているのはアプリ側の while 文」）と矛盾していた | 図を直したら、**同じページの他の記述・他の節の同じ図と突き合わせる** |
| 24 | 設問の見出しに「理解度チェック」「総合チェック」「解いてみる」の**3通りの名前**が混在し、一覧では「解いてみる」が6つ並んで区別できなかった | 同じ役割のものは**同じ名前**にして、違いは副題で出す |
| 25 | 全項目一覧を CSS の段組み（`columns`）で流したため、**読む順が縦→横に折れて追えなかった**。縦カードを並べた版では項数の差がそのまま高さの差になり隙間だらけになった | 一覧は**横長の帯を縦に積み、項目だけ中で段組み**にする（5v ④） |
| 26 | スクリーンショットを撮ったら**図が真っ白**だった。`scrollIntoViewIfNeeded()` で段階表示が再生し直され、その瞬間を撮っていた | 撮る前に **1.6秒待つ**（§3） |
| 27 | `matcher` が**出現0回**だった。§7 #9 で「用語を grep して数える」と決めたのに、**数える語の一覧を持っていなかった**ので、指摘されるまで気づけなかった。精査したら同種の穴が10件あり、うち1件（`stop_reason` の値が4つしかない）は欠落ではなく**誤り**だった | **決め事は検査に変換しないと守られない。** 必須語彙を `check.mjs` の 5g に持たせた。ブループリントの語を足したら、この一覧にも足す |
| 28 | 1-6 の本文に `</div>` が1つ多く、**節が早じまいして後半（ポイント・小見出し・詳細・ページ送り）が `.shell` の外に出ていた。** 見た目は「なんとなく余白が変」程度で、目視でも `check` でも見つからなかった | HTML の壊れは**表示が消えるとは限らない。** タグの釣り合いを `check.mjs` の 5h で数える。見つけたのは「画面に出ている font-size が6種類になった」という実測（`.sec h3.sub` が効かず UA 既定の 1.17em になっていた） |
| 29 | **100問中76問が、正解を選択肢の2番目に置いていた**（4番目はわずか1問）。「Bを選べば76%当たる」ので、練習として成立していなかった。設問を書くと正解を2番目に置く癖が出る | **書いた本人には見えない偏りがある。** はじめは全問を巡回させて 25/25/25/25 にしたが、それでは**書き足すたびに均衡を保つ手間**が要る。いまは `app.js` が**描画のたびに混ぜる**ので、偏りようがない。かわりに守るべき前提が「並びに依存する文言を書かない」に移ったので、`check.mjs` の 5i もそちらへ差し替えた |
| 30 | パンくずに「出題比率 27%（約16問）」を出していたが、**66ページすべてに同じ数字が出ていた**。読む順番を決めるときに1回使う数字で、節を読んでいる最中には要らない | **共通部に足すものは「毎ページに要るか」で判断する。** テンプレート（`reindex.mjs`）に1行足すと90ページに増える。配点の出所は<b>トップと 6-2 の図だけ</b>にした |
| 31 | 3-3 で図を比較表に差し替えたとき、**コードに打った丸番号 ①〜④ だけが残り、指す先が消えた**。逆向きの事故もあった ── 印が無いのに注記側に番号を足していた（1-7 の ⑥、4-5 の ④） | **印と注記は必ず対になる。** `check.mjs` の 5j が `.figbox` 単位で集合を突き合わせる。注記は同じ箱の中の `<ol class="ann">` に置く（離れた `.ex` から引用する形にすると、対応が機械で追えない） |
| 32 | 節ページから `index.html` へのリンクが**2本**あった ── 左上のブランドと、目次の下の「全項目一覧（90項）」。iPhone では「目次 … ▼」のすぐ下に並び、似た見た目のナビが2つ積まれていた | #30 と同じ穴を、**その直後にもう一度掘った。** 共通部に導線を足す前に、**すでにある導線で足りないか**を数える |
| 33 | 前後の送りに「前へ／次へ」の札・節名・「4 / 18」の位置表示を全部入れていたが、**節名も位置も左の目次が同じことを（しかも現在地を太字で）示していた** | 1ページの中でも同じ情報を3つ持たない。いまは**矢印だけ**（読み上げ用の文字は `.vh` で残す）を上下2か所に置いている |
| 34 | 3-5 のコマンド定義に `.k`（JSON のキーの色）を当て、書き直したときに「YAML frontmatter」の語を落とした。結果、**YAML なのに JSON に見え**、「キーにハイフンは使えるのか」と問われた | **色分けを流用するときは、何の記法かを文字で書く。** `check.mjs` の 5g に `YAML frontmatter` を入れた |
| 35 | iPhone で**節見出しに届くまでに 317px（画面の38%）**使っていた。上部ナビ60 ＋ 目次ボタン84 ＋ パンくず77 ＋ 送り74。しかも**パンくずは、すぐ上のボタンと同じ「Domain 1 …」**だった | 高さは1つずつではなく**積み上がりで測る。** いまは **126px**（15%）── パンくストを消し、`← 目次 →` を1行に重ねた。重ねるときは ① 帯に `pointer-events:none`（**下のボタンが押せなくなった**）② 的の幅ぶん左右を空ける（**矢印が「18 項 ▼」を隠した**）③ `.toc` ごと詰めない（**開いた一覧まで内側に入った**）── **3つとも実測で初めて分かった** |
| 36 | 理解度チェックの副題を「その区分に2つ以上あるときだけ」と決めた結果、**25件中2件（0-6・2-11）だけが「理解度チェック」で終わって**いた。とくに Domain 2 のものは**教材で最大の設問数**だったのに、一覧では文が途切れて見えた | **例外つきの規約は、その例外がそのまま不揃いになる。** 数で条件を切らず、**全件に付ける**。`check.mjs` の 5a2 も「必ず副題」に変えた |
| 37 | **検査が何も見ていない状態になっていた。** 検査1（図の固定色）は走査が `<svg>` の中だけで、その `<svg>` を本文から全廃したので対象が0件になった。それでも `✓ すべて var() 参照` と出るので、**通っているのか、見ていないのか区別できなかった** | **検査は「対象が何件あったか」を出す。** 0件なら通ったのではなく見ていない。いまは `✓ 本文の inline style / SVG 属性 121 箇所すべて` のように件数を書く。同じ理由で検査2（text の座標）も出番がなくなっている |
| 38 | §5 に「注記は16字を目安」と書いていたが、**実測の中央値は17字**だった。目安がそのまま守られていない状態が続いていた | **目安は実測から決める。** 全90項の分布（中央・75%・90%）を測って目安を書き直し、`check.mjs` の上限は「明確に文が入った」ものだけを捕まえる高さ（`.fig-h` 24字 / `.fig-note` 48字）に置いた。48字を超えた5件のうち**3件は長さではなく構造の問題**（`・A ・B ・C` を1つの注記に詰めていた）で、`<ul>` にした |
| 39 | トップページに**同じドメインの数字が2つ**出ていた ── 配点の図が「Domain 1 … 16問」、全項目一覧が「Domain 1 … 18項」。§1 で「番号が2系統ある、混同しないこと」と自分で書いておきながら、**数字でも同じことをしていた** | 一覧の項数を削除。**すぐ下に項目が並んでいて数えられる**うえ、行動が変わる数字でもない。同じ面に似た数字を2つ置かない |
| 40 | 設問数の按分を直すとき、**不足分をそのまま足すと合わない。** 足せば総数が増え、期待値（総数×比率）も動くため | **基準を1つ固定してから総数を逆算する。** Domain 3（20問＝20%）を据え置くと総数は100問と決まり、そこから各ドメインの目標が出る（結果 +29問）|
| 41 | `h3.sub`（節の中の小見出し）を 16px にしていたが、**本文と同じ大きさなので、青い左罫と上余白だけが手がかり**だった。文字サイズは5段階しか使わない決まりなので、**16px の上は 26px しかない**（＝ `h2` と同格）── だから長く判断待ちになっていた | **段階を絞った制約は、選択肢も絞る。** 実際に3案を同じ位置で撮って並べ、利用者に選んでもらった（26px ＋ 左罫を残す）。`h2` との区別は**項番号の有無と位置**で付く。390px でも最長2行に収まることを実測済み |
| 42 | **Task 2.5 を読み違えて、2-10 を作り替えてしまった。** 「組み込みツール」を API 側（`web_search` などのサーバツール）の話だと判断し、元にあった Claude Code の道具の説明を脇へ寄せた。公式 Exam Guide の原文は `Select and apply built-in tools (Read, Write, Edit, Bash, Grep, Glob) effectively` で、**元のほうが正しかった** | **タスクの解釈は、名前から推測せず原文を読む。** 原文は §8 の PDF にある。しかも「`Edit` が一意に当たらないときは `Read`＋`Write`」「`Grep` で入口→`Read` で追う」という**出題される中身を落としていた**。`check.mjs` の必須語彙も私の誤読で作られていたので、原文に合わせて直した |
| 43 | 公式 Exam Guide を読むまで、**本番に「複数選ぶ」設問があることを知らずに**教材を作っていた。設問100問すべてが4択1答で、`check.mjs` の 5i でも「4択であること」を**規約として固定**していた | **試験の形式そのものも原文で確かめる。** 出題範囲（何が出るか）だけでなく、**出題形式（どう出るか）**も Exam Guide に書いてある（Item format / Exam structure / Passing score）。いまはトップと 6-4 で注意を促すに留め、engine の対応は保留 |
| 44 | **JSON を、JSON と名乗る前に出していた。** 「JSON」の語の初出は 1-7 なのに、JSON のコードは 0-4（キャッシュ）に出ていて、しかも通常形の `"system": "文字列"` を飛ばして**応用形（ブロックの配列）から**入っていた。「ブロック」の語の初出も 0-4 で、説明は6節あとの 1-7。1-6 の `content[ tool_use ]` に至っては、`content` が配列だと知らないと読めない | §7 #9 で決めた「用語を grep して数える」は**語だけを数えていて、記法を数えていなかった。** 語彙が既習でも、**その語が乗っている入れ物**（JSON の形・図の記法）が未習なら読めない。前提に 0-3「送っているものの形」を足して、`messages` の配列・`{role, content}`・`content` の2通りを先に置いた。0-1 の図も「シーケンス図」と名乗らせた（1-6 が「シーケンス図で」と参照していたのに、記法名が一度も出ていなかった） |
| 45 | 設問数を各ページの `.lead` に**手書き**していて、29問足したあと**9件がズレた**。しかも `app.js` が `.lead` を**丸ごと差し替えて**いたため、画面上は正しい数が出る代わりに、**節ごとの一文が消えていた** | **数は data から、文は HTML から。** `.lead` に空の `<span class="qcount"></span>` を置き、`app.js` はそこにだけ数を入れる。上書きで辻褄を合わせると、**古い表記が検索インデックスに残り、手で書いた文が消える**という二重の事故になる（5v ⑥） |
| 46 | 25ファイルの一括置換に `perl -0pi -e "s|…|\Q$LEAD\E|"` を使い、`\Q…\E` が**全バイトをエスケープして UTF-8 を壊した**。復旧しようと `git checkout -- $(git status --porcelain \| awk '$1=="M"')` を打ったら、**同じターンで入れた意図的な編集3ファイルまで巻き戻した** | **一括置換はシェルの置換器ではなく `./tools/x node` で書く**（日本語を通すし、件数を出せる）。**`git checkout` に渡す一覧は、打つ前に中身を表示して確かめる** ── `$1=="M"` は「自分が壊したファイル」ではなく「変更のある全ファイル」に一致する |
| 47 | **必須語彙74件が全部通るのに、ブループリントの穴が11件あった。** 語彙リストを**公式 Knowledge / Skills から写さず、私の要約から作っていた**（3.5 は公式の4項目がひとつも無く、代わりに 3.1 の内容を教えていた） | #27 で「決め事は検査に変換する」と書いたが、**変換した検査の中身が原文由来でなかった。** 5g の語彙を原文から写し、5g2 を新設。**5g2 は当たった節も出す** ── 「テストを先に書く」が別の節にあった、という**置き場所ちがい**は存在チェックでは通るため |
| 48 | 6-1 の図の題が「1周を**左から右へ**」、説明が「**左から。**」「**中央の** Claude」「**右へ。**」だった。`--cols:4` は 390px で1列に折り返すので、**iPhone では左も中央も右もない**。§7 #2 でまったく同じことを直したのに、別の節で再発していた | **位置で指すのを禁じるだけでは足りない。検査に変える。** 折り返す図（`--cols:2` 以上を含む `.figbox`）の中と、その節の説明文から位置表現を機械で洗い出した ── 6-1 に4件、4-8・4-7・5-2 に各1件。いまは**箱の名前と丸番号**（①〜④）で対応させている。「横に並べる／縦に重ねる」（4-14）や「真ん中は読み落とされやすい」（5-2）は**入力の中の位置**を指す概念語なので対象外 |
| 49 | 模擬シナリオが「状況200字のページ」＋「別ページの設問」に分かれていて、**答えるときに状況が画面外**だった。しかも 6-4 は「7つの状況で試します」と書きながら、**その7つをどこにも列挙していなかった** | **設問がその場の文脈に依存するなら、同じ項に置く。** 7シナリオを1項に統合（14項 → 7項）。`app.js` の選択子を `.sec-quiz[data-quiz]` から `[data-quiz]` に変え、`check.mjs` の「見出しは理解度チェック」規約は**設問だけの項**（`quizOnly`）に限定した。6-4 には7本の一覧と、公式6本との対応表を置いた |
| 50 | 泣き別れ（`.point` の最終行が「る。」だけ）を消そうと `text-wrap:balance` を当てたら、**行末の空きが60px超で27件・最大268px**になった（当てる前は1件・最大189px） | **見た目の直しは、直った数と壊れた数を両方測る。** balance は行の長さをそろえるので、2行の段落では**両方の行が短くなる**。数字は `style.css` のコメントに残した（再挑戦するときの基準・5v ③） |
| 51 | 統合した項の設問に「理解度チェック ─ 社内の問い合わせ」という小見出しを付け、**ページ見出しと丸かぶり**した。§4 に規約があり、同じ理由で8件削除した経緯まで §7 に残していたのに再発 | **統合した項の設問は「理解度チェック」と名乗らせない**（いまは「では、どう手を打つか」）。その名前は**独立した確認の項**に取っておく。あわせて「N個ある」と書いたら**その N 個を並べる**。本文にリンクを書いたので 5l を足した |
| 52 | 6-2 の帯グラフで、**同じ「3本」の帯が 99px と 149px で描かれていた。** `.fig-bar` が行ごとに独立したグリッドで、列幅が `auto` だったため、**値の文字数が違う行だけトラックが伸び縮み**していた（トラック実測 200px と 301px）。長さを比べるための図なのに比べられない | **比率の図は、帯の実寸をブラウザで測って比を検算する。** 「幅の % が正しい」だけでは足りない ── **トラックが同じ長さでなければ % は意味を持たない。** いまは名前と値を1行目、トラックを2行目の全幅に置いて、幅に関係なく全行そろえている（`.fig-bars` を使う3図すべてに同じ不具合があった）。あわせて緑・赤で「多い／少ない」を表していたのを **`data-domain` のドメイン色**に直した ── 緑＝推奨・赤＝危険の対応を壊すのは §7 #22 と同じ誤り。トップの配点帯と同じ色になった |
| 53 | 「このドメインについて」が5項あるのを「Domain X について」に直すべきか問われ、**測ったら直す必要がなかった。** 390px で見出しの108px上に「Domain 2 ツールと MCP」が出ており、検索結果には領域チップ、`<title>` には `─ Domain 2 ─`、全項目一覧は領域名の見出しでグループ化されている ── **領域名なしで同名が並ぶ面が1つもない。** 変えると `2-1　Domain 2 について` で番号が2系統隣接し（§7 #12）、同じ画面に「Domain 2」が3回出て（#30/#32/#33）、ドメイン名の出所が2か所になる（§2） | **「同名が並ぶと区別できない」は、並ぶ面が実在するときだけ問題。** §7 #24 の設問25件は1つの平坦な一覧に並んでいたが、これは違う。**直す前に、その名前がどこに出るかを全部数える。** 「副題を足す」案も、すぐ上のグループ見出しの言い換えになるので採らなかった |
| 54 | 狭い画面の目次ボタンが `cur.textContent` をそのまま使っていて、項番号とタイトルが地続きだった。6-1「1枚で全体を見る」が **「6-11枚で全体を見る」＝「6-11」に見えていた**（#53 を測っている途中で見つかった） | **2つの span を結合するときは区切りを入れる。** `.itm-n` と `.itm-t` から取って全角空きで繋ぐ。`textContent` は要素の境界を消すので、番号と文字列を並べる場所では使わない。**確認のために測ると、聞かれていない欠陥が出てくる** |
| 55 | この #53/#54 を CLAUDE.md に書く置換が**バックティックのエスケープ失敗で落ちたのに、そのまま commit・push した。** `app.js` の修正だけが入り、記録が残らなかった | §7 #13（置換したら結果を grep する）を**自分で破った。** `./tools/x node -e` にテンプレートリテラルで長い日本語＋バックティックを渡すのは壊れやすい ── **表の行のような長文は `Edit` で入れる。** commit の前に `grep -c` で入ったことを確かめる |
| 56 | 390px で **`pre.code` の34ブロック中25が横スクロール**していた（最大 462px ＝ 幅の2倍以上はみ出す）。しかも `.figbox` 自体は溢れないので **`app.js` のスワイプ案内も出ず、切れていることに気づけない。** `measure` の「横溢れ0件」も通っていた ── あれはページの横溢れを見ていて、**要素の内側スクロールは見ていない** | **「溢れていない」と「読める」は違う**（§7 #11 と同じ構図）。`overflow:auto` を付けた要素は、溢れずに**中身を隠す**。≤640px で `white-space:pre-wrap` にして 25 → 0 にした。総スクロール量は 270k → 271k px でほぼ増えない。**要素ごとの `scrollWidth > clientWidth` を数える**のが正しい測り方 |
| 57 | 同じ回に「1行あたり17字は短すぎるのでは」と考えて余白を詰める案を測ったが、**17.0 → 17.2字にしかならなかった**（`.shell` 16px・`.figbox` 12px・`.point` 22px を詰めた場合） | **直す前に、直したときの数字を出す。** 390px で16pxの文字なら1行20字前後が上限で、**これは幅の制約であって余白の問題ではない。** 効かない変更を入れずに済んだ。「見づらい」の原因が版面なのか幅なのかは、A/B を測れば分かる |
| 58 | 「ブループリントを全部網羅しているか」と問われ、**「はい」と言えなかった。** #47 で照合したつもりが、当てたのは**そこから私が抜き出した92件**で、原文の箇条書きは約163項目あった。残りを当てたら**20件の穴**が出た | **「照合した」と言うときは、分母を原文の項目数にそろえる。** #47 で「要約から検査を作った」ことは直したのに、**照合の範囲そのものが私の選択だった**ことは残っていた ── 同じ誤りが一段上で再発する（5g2） |
| 59 | その163項目を `check.mjs` に流し込むとき、**正規表現リテラルで書き出して構文エラー**にした（`/~/.claude/CLAUDE.md|…/` の `/` が未エスケープ）。さらに検証用の `grep -o '.\{0,90\}…'` を**1行に固めた巨大な HTML** に投げてセッションを2回落とした | **生成する正規表現は文字列＋`new RegExp` で書く**（`/` のエスケープを考えなくてよい）。**`docs/*.html` は1行が数万字あるので、`grep -o` の広い文脈抽出を投げない** ── 中身を見るときは `./tools/x node` で本文を抜き、`slice` で前後を切る |
| 60 | 「改訂に追随できるか」と問われ、**壊して試したら7つのうち5つが黙って通った。** 比率を 27%→35% にすると**帯の長さだけ動き、問数のラベルは古いまま**残る。`weightFig` の中の「差は 7 問」まで手書きだった | **「単一の出所」は、数字を持つことではなく数字を導出すること。** 比率と問数を並べて持った時点で、両方を直す約束が発生している。いまは比率だけを持ち、問数・帯・差はすべて計算で出す（§8・5m・5n）。**仕組みを直したら、壊して落ちるかを必ず試す** |
| 61 | 「結局ブループリントを完全に網羅できたのか」と問われ、**原文の箇条書きを数えたら 240項目**だった。私が「約163項目」と言っていた数は、**原文を数えた数ではなく、私が書き出した probe の数**だった。240 と 163 は1対1でないので「全部確認した」とは言えない状態だった。残り77項目を書き起こしたら**4件の穴**が出た（`resources` を出す目的／個人用スキルを `~/.claude/skills/` に別名で／プロンプト側にも書式をそろえる規則／締め切りから投げる間隔を逆算） | **「何件確認したか」を言うときは、分母が原文から出た数かを確かめる。** §7 #47 → #58 → ここまで、**同じ誤りが3回、一段ずつ上に現れた**（要約から検査を作る → 照合の範囲が私の選択 → 分母が私の probe 数）。いまは `blueprint.mjs` の `concepts` が**原文の箇条書きと1対1（240）**で、`check` が `✓ 240 項目` と数を出す。**分母を原文に固定すれば、この誤りは繰り返せない** |
| 62 | 「原文と同じくらい価値があるか」と問われて調べたら、**ガイドは39ページ・18章で、私は §1〜§8 しか読んでいなかった。** CLAUDE.md に「全24ページ」と自分で書いて、そこで止めていた。§17 の Out-of-Scope に照らすと、**教材はストリーミングを図2本、キャッシュの実装詳細を1節まるごと教えていた** | **「網羅」は穴だけの話ではない。範囲外を厚く教えるのも同じくらい悪い** ── 出ない内容に割いた行数はそのまま損になる（5o）。あわせて**ページ数を推測で書かない** ── 「全24ページ」は確かめずに書いた数で、それが**2回の監査の天井**になっていた |
| 63 | §9 の例題12問を教材と突き合わせたら、11問は当たったが **Q7（親の分解が狭すぎて話題を取りこぼす）だけ、4-8 の無関係な文に偶然当たっていた。** Domain 1 には実質その内容が無く、「当たった節も出す」検査があったから気づけた | **存在チェックは「どこで当たったか」まで出す。** 出していなければ「○」で通っていた（§7 #58 で入れた仕組みが、ここで効いた）。いまは 1-11 に「子は割り当てられた範囲しか見ない。失敗を疑うときは、まず親の分け方を見る」を置き、probe も置き場所ごと当たる語に直した |
| 64 | ガイド全18章を通しで教材と突き合わせたら、**20項目のうち15項目が入っていなかった。** §6（出題される中身）ばかり見ていて、**§1 何の試験か・§2 想定読者・§5 シナリオの本文・§7 準備・§8 演習・§9 例題12問・§11〜§15 受験の実務**を一度も照合していなかった。とくに **§9 の「解説つき例題12問が公式にある」ことを伝えていない**のは、教材の128問より権威がある材料を読み手から隠していたのと同じ | **「網羅」の物差しは §6 だけではない。ガイドは全部が読み手向けの情報。** いまは `blueprint.mjs` が `PREPARE`（§7 の7項目）・`EXERCISES`（§8 の4演習）・`SCENARIOS[].detail`（§5 の本文）・`EXAM` の受験実務まで持ち、`<!--#preparefig-->` と `<!--#examadmin-->` で差し込む。**§11〜§16 は「行動が変わる数字だけ」に絞った**（受験料・待機日数・有効期間・当日の持ち物）── 支払い方法や苦情の窓口は公式へのリンクで足りる |
| 65 | この回、`node -e` のスクリプトで**置換を配列で回し、最後に1回だけ書き出す**形にしていた。2件目で `process.exit(1)` したため、**成功していた1件目も保存されずに消えた**。しかも「○ …」とログには出ていたので、通ったように見えた | **途中で落ちうる一括処理は、1件ずつ書き出す。** まとめて書くなら、**ログは書き出しの後に出す。** 「○」が出ているのに入っていない、が起きる |
| 66 | CLAUDE.md を監査したら、**書いてある数字が7件ズレていた**（項数・図・コード・`.ann`・本文の節数・検査の項目数・節名）。さらに **§11 の按分表が「差 0」と書いてあるのに、範囲外の設問を削ったせいで Domain 4 が崩れていた** | **CLAUDE.md の数字も検査に変換する**（5p）。CLAUDE.md は毎セッション読み込まれるので、古い数字を前提に作業が進む。**理想は数字を書かないこと**だが、書くなら検査を付ける ── `<span class="qcount">` や `<!--#nsec-->` と同じ考え方 |
| 67 | 同じ監査で、**§10「引き継ぎ ─ いまの状態」がセッションごとの作業ログの山になっていた**（7,218字・全体の15.5%）。「いまの状態」と題しながら、中身は「2026-08-09 に○○を直した」という経緯で、しかも数字を持つぶん古くなり続ける（上の7件のうち5件が §10 にあった） | **経緯は git log にある。CLAUDE.md には結論だけ置く。** §10 を6行の表（いつ・何を・どこに記録）に圧縮した（7,218 → 3,676字）。**「いまの状態」を名乗る節に、日付つきの作業記録を足さない** |
| 68 | 「§9 の例題12問を訳してテスト形式で足すとよいか」と問われて測ったら、**12問すべてが既習範囲で、知識としては新しいものが1つも無かった。** 訳して載せるのは3つの理由で採らなかった：① 正解が英語の限定語（`first step` 等）で決まるので**訳すと別の選択肢も正解になる** ② 本番は英語で、**原文を英語で解くこと自体が練習** ③ リポジトリが public で、全文翻訳は再配布にあたる | **原文の価値は「問題」ではなく「解説」にあることがある。** 訳は再配布になるが**分類は分析**なので再配布にならず、しかも12問ではなく**本番60問に効く**。誤答36個を6つの型に分けて 6-13 に置いた。数字は `SAMPLES` から導出し、手書きの「例題N問」も 5m が突き合わせる |
| 69 | 6-4 にあった「2つの誤答パターン」を消したら、**無関係な検査が2件落ちた** ── 5.2 の概念が、消した文の「**複数**エージェントにする」「毎回人が**確認**する」に**偶然当たっていた**。本来あるべき節には無く、タスク名の**後半が丸ごと欠けていた** | **無関係な編集で検査が落ちたら、それはその検査が偶然通っていた証拠。** #63 と同じ事故が同じ検査で2回目 ── 5g2 は**全本文**を走査するので、当たるべき節の外で当たっても通る。当たった節を出す仕組み（#58）だけでは、**本来の節に無いこと**は気づけない。**将来は概念 probe を `sections` に限定して照合するのが本筋** |
| 70 | 「あちこち足したり消したりしたので冗長になっていないか」と問われて全体を測ったら、**冗長はほぼ無かった**（散文の22字以上の重複は24件・実質4件、`.point` の18字以上一致は0件、節の分量は中央1,030字で外れ値なし、先行参照はすべて「Domain N で扱う」と明示済み）。かわりに**設問の間隔が極端に不揃い**だった ── Domain 3 は2〜3項ごとに4問なのに、**Domain 2 は8項読んでから18問**（教材で最長の 9,857px）、Domain 5 は8項読んでから15問。読み終えた頃には最初の節が遠い | **「冗長か」を測るなら、文の重複だけでなく〈確かめるまでの距離〉も測る。** 設問は1問も増やさず、置き場所だけ変えた ── Domain 2 の18問を内容で 5/6/7 に割って本文の途中へ（9項→11項）、Domain 5 は設問ページを前へ移すだけ（設問データは変更なし）。最大の間隔 8項→7項、最長ページ 9,857→7,023px。**割り方が正しいかは 5（出題範囲）が自動で検証する** ── 移した先で未習の内容を問うていれば落ちる |
| 71 | 0-5 プロンプトキャッシュが **1,521字かけて崩れ方3つ・積まれる順・有効期限まで教えたあと、末尾で「覚えなくてかまいません」と書いていた。** 公式 §17 が範囲外としている内容で、**払わせた読む時間を自分で否定する形**になっていた | **範囲外を「教えてから断る」のは、教えないより悪い。** 性質と2つの橋渡しだけ残して 31% 減。**#62 で入れた 5o は「範囲外を扱う節がいくつあるか」を見るので、節の中の厚みは見ていない** ── 5o が通っていても厚さは残る |
| 72 | 1-8 と 1-18 の設問も割ったら、**Domain 1 の16ファイルを振り直すことになり、CLAUDE.md の節参照が23件古くなった**。本文側は生成ブロックが**節 id から解決する**ので無事だったが、手書きが1件だけあり 5l が捕まえた | **節を1つ挿すとドメイン内の番号が全部ずれる。番号での参照は検査に変える**（5q）。**§7 の記録も番号を追随させる** ── 指す先が解決しない記録は再発防止に使えない。**本文から節番号で参照しない**（リンクにすれば 5l が見る、生成ブロックなら id で解決する） |
| 73 | 「どのページも不親切すぎる」と指摘されて測ったら、**そのとおりだった。** ① 導入が**専門用語や前方参照から始まる**節が22項 ② **位置参照が11件**（「前節の関門は…」）── #48 で「検査に変える」と決めたのに、**入れた検査は `.figbox` の中しか見ていなかった** ③ 用語をのべ**146箇所**で既知として使うのに、**他の節への案内リンクを持つ本文の節は実質1つ** | **導入の第1文は、その節を知らない人に通じる1文にする。** 22項を書き直した。**位置で指さず、節の名前でリンクする**（5l・検査3）。**「検査に変えた」と言うときは、走査範囲が本文全体かを確かめる** ── #48 はそこで抜けた |
| 74 | 「トップページ最下部が読みづらい」と撮ったら、〈参考資料〉の3列表が 390px で**カード9枚 × 3段**に畳まれ、**情報を持たないラベルが9回**繰り返され、**「API」で始まるカードが4枚連続**していた | **カタログを比較表で組まない**（比較表は左右が同じ観点を述べるときの形・#14）。種類を見出しへ上げ、**2列 × 3表**に組み替えた（5v ⑤）。直す途中で2つ踏んだ ── ① **`.hero` に `h3` の指定がなく、UA 既定で文字サイズが6種類**になった（#28 と同じ合図）② 幅を `#id` で指定したら **`@media (max-width:640px)` より強く**、狭い画面でも効いて名前が3行に折れた。**`#id` でレイアウトを指定するときは `min-width` で囲う** |
| 75 | 「グレーの縦棒とグレーの見出しの領域が見づらい」と指摘されて測ったら、**`.ex` の左罫は地との対比が dark 1.52 / light 1.21** ── 罫があること自体が見えていなかった。**面を持たない代わりに左罫だけで区画を示す設計なのに、その唯一の手がかりが見えていなかった** | **区画を示す罫は、地との対比を測ってから決める。** グレーは罫として機能しない ── 示すならアクセントか赤、示さないなら置かない。`.ex` をアクセントに上げ、**同じ見た目が2つ並ばないよう `.goal` は罫を捨てて見出し直下の1行**にした（5c が位置も見る）。**「AA を通っている」は「見えている」ではない** ── コントラスト検査は**文字**を見ていて、罫は対象外だった |
| 76 | 続けて3件の指摘を受けて、いずれも**規約が既にあるのに守られていない**箇所だった。① `.exl` の中で `<b>` を使い、**色つきのラベルの中に別の色が混ざって**いた（5件）② 5-8 の表に**左セルが「─」の空行**があり、`.ok` / `.ng`（判定の札）を強調に流用していた ③ 5-9 が **3項目を `<br>` で1つの塊に詰め込んで**いた | **強調は色でやらない。「」で囲う**（5r）。**`.ok` / `.ng` は判定にだけ使う** ── 強調に流用すると赤＝誤り・緑＝正しいの対応が壊れる（#22 と同じ）。**「A か B か C か」を `<br>` で並べない** ── §5 の型どおり比較表にする。**3件とも、新しい規約ではなく既存の規約の破れだった** |
| 77 | 「新たな知識を学習する箇所は全て『そのゴール達成の最適な手段か？』を基準に判断すべき」と指示されて数えたら、**小見出し35件にゴールが無かった** ── その区画の説明が長いのか短いのかを測る基準がなかった | **ゴールは「読み終えて何が言えるか」であると同時に、書く側が「この説明はいちばん短い手か」を判断する基準。** 新しい知識を教える区画には必ず置く（5c が小見出しまで見る）。書き足すとき**3件で既存のゴールと内容が重なった** ── **ゴールが重なるのは、区切り方が内容と合っていない合図** |
| 78 | 2-6 の「作る量が『掛け算』から『足し算』になります」が理解できないと指摘された。読み返すと、**掛け合わせる対象（アプリ × サービス）と「本」が何を指すかが本文に一度も書かれておらず**、図の中で急に「9本」が出ていた。これは比喩ではなく**実際の算数**なので、数える対象が要る | **数の話をするなら、何を数えているかを先に言う。** 導入を「アプリとサービスを繋ぐ実装を、組み合わせのぶんだけ書かずに済ませる規格」に変え、3×3＝9本／3＋3＝6本を本文に出した。図の題も「1本＝『あるアプリから、あるサービスを使う』ための実装」と定義から始めた。§7 #1 の「たとえは図と1対1で対応するときだけ」の裏返し ── **たとえに見えて実は算数のものは、定義を落とすと何も伝わらない** |
| 79 | 「以前『不親切すぎる』と言ったのは、2-6 の『掛け算』のようなこと」と補足された。**「読者が組み立てられない主張」**のことで、私が前に直した「導入の第1文」とは別の層だった。同じ形を探して**図43点の題と結び・数を伴う主張を全部読んだ**ところ、単位を定義していないのは 2-6 だけだったが、**助数詞の揺れが3件**見つかった ── `stop_reason` の値を「2本」、道具を「8個」と「1本ずつ」で混在、窓を「同じ1本」（帯は図の表現であって窓ではない） | **数や量を主張するなら、何を数えているかを先に定義する。助数詞は対象ごとに固定する**（道具＝個／つ、往復＝本、シナリオ＝本）。**「不親切」には層がある** ── ① 何の話か分からない（導入・§7 #73）② 用語の出口がない（146箇所・未着手）③ **主張の材料が本文にない**（ここ）。①を直しても③は残る。指摘を受けたら、**どの層の話かを先に確かめる** |
| 80 | 「丸付き数字が円の下よりになっている」と指摘され、**画素で測ったら中心から 2.63px 下**にあった（円は17px）。原因は `line-height` を書いていないこと ── 丸は `place-items:center` で中央そろえしているが、**中の行ボックスが親の行送りを継ぐ**ので、`pre.code`（1.85）では行ボックスが円より高くなり数字が沈む。4つある丸のうち `line-height:1` があったのは1つだけだった | **丸の中に文字を置くなら `line-height:1` を書く**（5s）。さらに数字はベースラインに乗るので `padding-bottom:1px` で上へ寄せる。結果 **2.63 → 0.13px**。**「中央そろえ」を指定しても中央に来るとは限らない** ── 中央に置かれるのは行ボックスであって、字ではない。**測り方**：要素を高倍率で撮り、画像を canvas に戻して**円の水平範囲の内側だけ**で暗い画素を数える（背景まで数えると円の外まで「数字」になる） |
| 81 | 「矢印キーでもページ移動できるように」と頼まれ、`keydown` の待ち受けを**もう1つ足した**。「入力中か」の判定が**検索側と矢印側で二重**になっていた ── 片方だけ直す事故が待っている形 | **同じ入り口の機能は、入り口ごと1か所にまとめる**（5t）。`typing()`・`searchOpen()`・`pager(dir)` を共有し、`/` `Esc` `←` `→` を1つの中に並べた。挙動は10通りを実測。**`←` `→` を取り上げてよいのは、この教材が全幅で横溢れ0だから** ── 奪う操作があるかを先に確かめる |
| 82 | 「英語の語彙を学ぶページを作れるか」と聞かれた。**作れたが、私が英語を書いてはいけない類の仕事だった** ── それらしい訳語を並べると、本番に出ない語を覚えることになる。数えたら**タスク名30件は13個の動詞で始まって**おり、In/Out of Scope と §17 の技術一覧を合わせれば**原文だけで語彙表が組める**と分かった | **原文から取れる語彙は、原文からしか取らない**（5u が対訳の英語の実在を照合）。動詞の一覧は `TASKS` から導出し、知らない動詞が来たら `reindex` が throw する。あわせて §17 の技術一覧を取り込んだので、ガイドの網羅も一段上がった |
| 83 | 6-14 を1ページに詰めたら **390px で 12,299px**（それまでの最長 7,023px の1.75倍）になった。`data-l` の繰り返しが**73回** ── 2列の表で「左が語・右が説明」と自明なのに、畳んだカードすべてにラベルが出ていた（#74 と同じ形） | **2列の表で `data-l` を使うのは、左右が対等な観点のときだけ**（5v ⑤）。73件を落とし、2項に分けて 4,279px / 6,842px にした。**新しいページを作ったら 390px の高さを測る** ── 中央 2,746px・最長 7,023px が今の目安 |
| 84 | 3点まとめて指摘された。① **本文に「`check` が毎回照合します」と書いていた** ── #14 で「実装の話を持ち込まない」と決めたのに再発 ② 対訳表を**日本語 → 英語**の順にしていたが、**本番で目に入るのは英語のほう** ③ **`scratchpad files` は名詞なのに動詞で訳していた** ── 数え直したら54組中**35組**が同じ形だった | ① **道具の名前は本文に出さない**（検査3を本文全体へ広げた。**本文だけを見る** ── ファイル冒頭の編集マーカーに道具名が書いてあるので、ファイル全体を走査すると全ページが落ちる）② **並べる順は、読み手が先に見るほうを左に** ③ **英語が名詞句なら、日本語も名詞句にそろえる**（5u）。**訳は品詞まで合わせないと、覚えたものが本番の語と結びつかない** |
| 85 | #84 で対訳表の列を入れ替えた（日本語 → 英語 を 英語 → 日本語 に）ところ、**390px で横溢れが4件出た。** `.tbl tbody th` に `white-space:nowrap` が付いていて、日本語のときは自然に折れていたが、**長い英語（`sequential task decomposition into focused passes`）が折り返せなくなった**。しかも `check` は通るので、`measure` を見るまで気づかない ── そして**私はそれを push したあとに気づいた** | **列の入れ替えは「中身の移動」ではなく「別の見た目の箱への移動」。** `th` と `td` は指定が違うので、動かした中身がその指定に耐えるかを測る。`.tbl.pair tbody th{ white-space:normal }` を**幅の条件を付けずに**当てて解消（幅 34% だけを `min-width` に残した）。**見た目を変えたら push の前に `measure` を通す** ── `check` は静的検査なので、折り返しの破れは見えない |
| 86 | CLAUDE.md を監査したら、**§7 #84 が丸ごと落ちていた。** スクリプトが `t` に追記したのに書き出さず、そのあと**ファイルを読み直した `u` を保存**していた。しかも「○」とログには出していた（#65 で決めた、その通りの事故）。あわせて **#60 が #61 の後ろ**にあり、**§10 の見出しが日付を抱えていた** | **番号は「あとで参照する住所」。飛んだり乱れたりしたら気づけるようにする**（5p）。**「いまの状態」を名乗る節に日付を書かない** ── 書いた瞬間から古くなる。**同じファイルを2回読んで別の変数に入れない** ── どちらを書き出すかで結果が変わる |
| 87 | 「検査に変換してから §7 を畳んで」と指示され、85件を「検査で守られている／変換できる／できない」に分けた。**変換できたのは6つ**（`<details>` ／ 個別の `max-width` ／ `text-wrap` ／ `columns` ／ 2列表の `data-l` ／ 設問数の手書き）── どれも**入れても表示は壊れない**ので、目視でも `measure` でも気づけない類だった | **畳んでよいのは、検査が代わりに守ってくれる分だけ。** 6つを 5v にまとめ（項目を6つ増やさない）、検査のある27件を畳んで **§7 を 24,854 → 21,960字**、全体を 54,682 → 50,655字にした。**残る55件は畳まない** ── 判断・作法・測り方は検査に変換できず、**文章でしか守られていない**。畳むときに残すのは ① 具体的な失敗 ② 検査にできない洞察（測り方・判断）③ 検査 ID の3つ |
| 88 | 「PreToolUse を学ぶには『git commit の前に test を実行する設定例』があるほうがはるかに理解が早いのでは」と指摘され、数えたら**フックは5節・6,855字で教えているのにコードは2つだけ**、しかも1つは `check.sh` という**中身のない黒箱**だった ── **フックに何が届き、中で何をして、どう止めるかが一度も出ていない**。足したら 390px で 7,847px（#83 の目安 7,023px 超）になり、そこも直すことになった | **仕組みを教える節には、目的のある動く例を1本置く。** 「登録の仕方」だけでは読み手は自分で書けない。1-19 を新設し、`settings.json`（`matcher: "Bash"`）と `pretest.sh`（`tool_input.command` を見て `exit 2` で止める）を通しで置いた ── 公式 §8 の演習①も §9 Q1 の正解も**同じ形**なので、出題の中心でもある。**契約は公式ドキュメントで裏取りする**（届くのは stdin の JSON、止めるのは `exit 2` で `1` ではない、標準エラーがモデルに届く）。**1項に「仕組み」と「実物」を同居させない** ── 分けて 5,431 / 2,769px にした |
| 89 | #88 の続きで「同じ穴が他にもある」として 1-14 と 1-22 を挙げたが、**1-22 は穴ではなかった。** コマンド（`--continue` ／ `--resume` ／ `--fork-session`）は**比較表にすでに具体的に載っていた** ── 私が使った目安が「その節に `pre.code` が0個か」だったので、**表で具体を出している節まで穴と数えていた** | **「コードが0」は「具体例が無い」ではない。** §5 の判定どおり、区別は比較表・形はコード・手順は図で、どれも具体になりうる。**穴かどうかは「読んだ人が自分で書けるか」で測る** ── 1-14 は「子の定義ファイル」を3回名指ししながら実物が無く、読んでも書けなかった（実物を足した）。1-22 は表を読めばそのまま打てるので、足すと重複になる |

---

## 8. 事実確認の原則

**API の仕様は必ず公式ドキュメントで裏取りする。** 記憶で書かない。

この方針で、思い込みを2回訂正している。

- 「`messages` に `role:"system"` は入れられない」→ **入れられる**（新しめのモデルでは、先頭以外に限り、会話途中の system 指示として）
- 「コンテキストウィンドウはプランで決まる」→ **モデルで決まる**（プランは使用量の話）

**教材の記述そのものも測って確かめる。** トップの「読む順番」は「どのドメインから読んでも構いませんが、
前提だけは先に」と書いてあったが、**用語の初出を本文の全節で数えたら誤りだった** ──
`tool_use` `stop_reason` `hooks` サブエージェントの初出はすべて Domain 1 で、D2・D3・D5 はそれに依存している。
正しくは **前提 → D1 → D2 → D4 が固定順**、D3 と D5 は D1 のあとなら自由。

### ブループリント由来の事実は `tools/blueprint.mjs` が唯一の出所

**公式ガイドから来た数字・語・タスク名を、ここ以外に書かない。**

| 中身 | 使われ方 |
|---|---|
| `GUIDE` | 版（`1.0` / `July 2026` / `CCAR-F`）と PDF の URL。**改訂に気づくための記録** |
| `EXAM` | 60問・120分・720点・6シナリオ中4本・複数選択の有無 |
| `DOMAINS` | 5ドメインの**比率だけ**。問数は `domainItems()` が `EXAM.items` から計算する |
| `SCENARIOS` | 公式6シナリオと、それぞれの Primary domains |
| `TASKS` | 30タスク × { 原文 `name` ／ 対応節 `sections` ／ 必須語 `vocab` ／ 必須概念 `concepts` } |
| `TECH` | §17 の技術一覧14項目（**原文をそのまま**。訳さない） |
| `QUALIFIERS` | §9 の設問文から抜いた**正解を決める限定語**6件 |
| `GLOSSARY` | **公式の英語 ⇄ 教材の日本語** 54組。英語は原文からしか取らず、**日本語も名詞句にそろえる**（5u が両方見る） |

**`concepts` は公式 §6 の Knowledge of / Skills in の箇条書きと1対1（全240項目）。**
キーの `K1` / `S2` は原文での並び順。値は正規表現で、**配列なら全部満たすこと**
（1つの箇条書きが複数のことを言っている場合に使う ── 1.1 S3 の「誤り3つ」など）。
箇条書きの数はドメインごとに D1=48・D2=43・D3=49・D4=47・D5=53。

**問数・帯の幅・「差は N 問」・シナリオ別の本数は、すべて導出。手で書かない。**
以前は手書きで、比率を変えると**帯の長さだけ動いて問数ラベルが古いまま残った**（§7 #60）。

HTML 側は差し込みで受ける ── `<!--#weightfig-->`（配点の帯）／`<!--#examfmt-->`（60問・120分・720点）／
`<!--#scenariofig-->`（6-2 のシナリオ表と帯）。

### 改訂されたときの手順

1. `GUIDE.url` から PDF を取り直す（`WebFetch` は要約に失敗するが**本体をファイルに保存する**ので `Read` に `pages=` を渡す）
2. **§6 Detailed Objectives と `TASKS` を突き合わせる。** タスク名・Knowledge・Skills を原文から写す
3. `GUIDE.version` / `EXAM` / `DOMAINS` / `SCENARIOS` を直す
4. `./tools/x reindex && ./tools/x check` ── **落ちたところだけ本文を追う**

検査は次を見る（実験して、実際に落ちることを確認済み・§7 #60）：

- **タスクが増えた** → 5f と 5n が落ちる（対応節がない／`data-t` から参照されていない）
- **タスクが減った** → `reindex` が「未知のタスク番号」で止まる
- **比率が変わった** → 5m が落ちる（合計が 100% でない／問数の合計が `EXAM.items` と合わない）
- **出題数が変わった** → 5m が落ちる（按分の合計が合わない ＝ 丸め方を決め直せという合図）
- **シナリオが増減した** → 5m が落ちる（`EXAM.scenariosTotal` と本数が違う）

### 公式 Exam Guide は、この URL から取り直せる

Version 1.0 / Effective July 2026 / Exam code CCAR-F。**手元に無くなったら WebFetch で取る。**

```
https://everpath-course-content.s3-accelerate.amazonaws.com/instructor/6nizmqk8tpzpfjvt6qmmav7rh/public/1783542750/Claude+Certified+Architect+%E2%80%93+Foundations+Exam+Guide.pdf
```

`WebFetch` は PDF を要約できずに落ちるが、**そのとき本体を tool-results にファイルとして保存する**ので、
`Read` に `pages="1-8"` 等を渡せば中身が読める。認定ページは
https://anthropic-partners.skilljar.com/claude-certified-architect-foundations-certification 。
**このリンクは `docs/index.html` の〈参考資料〉にも置いてある**（読み手が原文を確認できるように）。

### **全39ページ・18章。最後まで読むこと**

以前ここに「全24ページ」と書いて §8 で止めていた。**読んでいなかった §9 に解説つきの例題12問、
§17 に出題範囲の公式リストがあった**（§7 #62）。ページ数を推測で書かない。

| 章 | 中身 | 使い方 |
|---|---|---|
| 3 | Exam Details | 60問・120分・**6本のうち4本**・720/1000 → `EXAM` |
| 4 | Blueprint | 5ドメインの比率 → `DOMAINS` |
| 5 | Exam Scenarios | **6本と Primary domains** → `SCENARIOS` |
| **6** | **Detailed Objectives** | **30タスクの原文＋ Knowledge / Skills（240項目）** → `TASKS` |
| 7・8 | How to Prepare / Preparation Exercises | 手を動かす準備 → `PREPARE` / `EXERCISES`（6-12 に出す） |
| **9** | **Sample Questions** | **解説つきの例題12問。** 教材の答えと突き合わせる ── 食い違えば教材が誤り |
| 10 | How the Exam Is Scored | 基準準拠・ドメイン別の正答率も出るが合否は総合点 |
| 11〜16 | 申込・規定・当日・NDA・更新・サポート | 受験の実務。教材の範囲外 |
| **17** | **Appendix** | **In-Scope / Out-of-Scope** → `SCOPE` ／ **技術一覧** → `TECH`（6-15 の対訳の出どころ） |
| 18 | Document Control | 版の履歴。改訂の確認に使う |

**§6 は必ず全部読む。** タスク名だけを見て中身を推測すると、`check.mjs` の必須語彙を
自分の要約から作ってしまい、74件すべて通るのに穴が11件ある状態になる（§7 #47）。

**§17 の Out-of-Scope も必ず読む。** 穴（出るのに教えていない）だけを見ていると、
**範囲外を厚く教えている**ことに気づけない ── 実際、ストリーミングのイベント列を図2本、
キャッシュの実装詳細を1節まるごと教えていた。`check.mjs` の 5o が見張る。

参照先：

- https://platform.claude.com/docs/en/build-with-claude/working-with-messages
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://platform.claude.com/docs/en/build-with-claude/extended-thinking
- https://platform.claude.com/docs/en/build-with-claude/streaming
- https://code.claude.com/docs/en/mcp
- https://code.claude.com/docs/en/hooks
- https://support.claude.com/en/articles/8606394-how-large-is-the-context-window-on-paid-claude-plans

モデル世代で変わる具体的な数値（200K / 1M など）は**本文に書かない**。
問われるのは「窓は共有資源で、入力と出力が同じ枠を食い合う」という構造のほう。

---

## 9. git の現状

ブランチ `main`、remote は `https://github.com/xeexi/ccaf-learn.git`。

### この WSL セッションから push が通る ── 2026-08-09 検証済み

`git push origin main` がそのまま通り、`12ccde0..87505f5` が remote に入った。
`credential.helper` は未設定だが、PATH に Windows の `git` があるため
**Git Credential Manager がそのまま効く**。プロンプトは出ない。

`GIT_TERMINAL_PROMPT=0` を付けて実行するとよい。資格情報が切れていた場合に、
入力待ちで固まる代わりにその場で失敗する。

**`user.name` / `user.email` が未設定のことがある。** その場合 `git config user.name` は
空を返して **exit 1** し、`&&` で繋いだコマンド列がそこで黙って止まる。
コミット時に一時指定すれば足りる：

```bash
git -c user.name=xeexi -c user.email=ki4shi.ito@gmail.com commit -F - <<'MSG'
…
MSG
```

### ほかの経路

| 経路 | push |
|---|---|
| **この WSL の Claude Code** | **通る（2026-08-09 検証）** |
| Code タブの **Local** セッション | **通る（2026-08-08 検証）**。Windows の設定をそのまま継承する |
| Code タブの Cloud | git proxy が 403 |
| **Cowork（マウント越し）** | **通らない。調べ直さないこと** ── 下記 |

**Cowork からは push できない。設定を探しても解決しない**（2026-08-08 裏取り済み）。
ローカル実行はサンドボックスに認証情報が一切なく、クラウド実行は git proxy が
`... is not in this session's authorized repository set` で 403 になる（PAT を渡しても同じ）。
Anthropic 公式リポジトリに未解決のバグとして上がっている ──
[anthropics/claude-code #76248](https://github.com/anthropics/claude-code/issues/76248)。

またマウント越しでは `.git/index.lock` が0バイトで残ることがあり、
`rm` も既定で `Operation not permitted` になる（削除許可を取ってから消す）。

**GitHub コネクタでも代替できない。** リポジトリを読み取ってコンテキストに入れる機能で、
commit も push も扱わない。

### GitHub Pages で公開する

成果物を `docs/` に置いてあるのは、**Pages がブランチ公開で選べるフォルダが「リポジトリ直下」と
「`/docs`」の2つだけ**だから。Settings → Pages で「Deploy from a branch」＋ `/docs` を選べば公開できる。
`docs/.nojekyll` は Jekyll を通させないための空ファイル。

注意：**Pages のサイトはリポジトリが private でも公開される。** また private リポジトリの Pages は有料プラン限定。

---

## 10. 引き継ぎ ─ いまの状態

**図を HTML で組み直す作業は完了した。** 前提・Domain 1〜5・まとめの全95項。
**本文に SVG は1点も残っていない。** 途中で**ブループリントの網羅を精査**し、穴を10件埋めた（下記）。

### 完了していること

- **図の作り方を SVG から HTML へ**（§5）。シーケンス図・比較表・帯・手順の部品を新設
- **iPhone の3件**を実測で修正 ── 折り返し（`text-wrap:pretty` を削除）、
  ページ移動ボタン（215px → 143px）、上部ナビ（横スクロール解消・タップ領域44px）
- **ボタンの見た目**（§7 #20）、**箱の階層**（§4）、**色の規則**（§5）を整理
- **トップページを整理** ── カード7枚を廃止して全項目一覧に一本化、配点を積み上げ帯に、
  読む順番を実測に合わせて訂正、比較表を4行→2行に圧縮
- **「ブループリント対応なし」24件・重複した `.point` 14件を削除**、設問の見出しを統一
- **`tools/x` と `tools/measure.mjs` を新設**（§3）
- **JSON の記法を、使う前に置いた**（下記）
- **公式 Exam Guide の Knowledge / Skills まで照合し、穴11件を埋めた**（下記）

### これまでに直した内容

**個々の作業の経緯は git log にある。**（`git log --oneline` で追える）ここには結論だけ置く。

| いつ | 何を | 記録 |
|---|---|---|
| 図を HTML へ | 本文から SVG を全廃。シーケンス図・比較表・帯・手順の部品を新設 | §5 |
| iPhone 対応 | 折り返し・ページ送り・上部ナビ・コードの折り返しを実測で修正 | §7 #19 #35 #56 |
| ブループリント網羅 | 公式 §6 の箇条書き **240項目と1対1** の検査に。穴を35件埋めた | §7 #47 #58 #61 |
| 範囲の見直し | 公式 §17 の Out-of-Scope に照らし、ストリーミングとキャッシュの実装詳細を削った | §7 #62 |
| ガイド全18章 | §1・§2・§5本文・§7・§8・§9・§11〜§15 を教材に入れた（6-12 を新設） | §7 #64 |
| 公式の例題12問 | 訳さず、誤答36個を6つの型に分類して 6-13 を新設。12問→戻り先の対応表つき | §7 #68・#69 |
| 全体の構成 | 設問の間隔を揃えた（Domain 1 を6か所・2 を3か所に分割、5 は移動）。0-5 の範囲外を削った | §7 #70〜#72 |
| 出所の一本化 | 公式由来の事実を `tools/blueprint.mjs` に集約し、数字を導出に | §7 #60 |

### 数字（`./tools/x measure --all` の実測）

| | 着手前 | いま |
|---|---|---|
| SVG の図 | 63点 | **0点** |
| HTML の図 | 0 | **43点**（ほかに比較表 39・コード 35） |
| 図の外の `.ann` | 61 | **16**（すべて `pre.code` の注記。図の外の番号説明は全廃） |
| 横スクロールが要る図（390px） | 63 | **0** |
| 横溢れ（390 / 640 / 1024 / 1440px） | ─ | **0件** |
| 図の中の最小実効サイズ（390px） | **4.5px** | **12px** |
| 文字サイズの種類 | ─ | **5種類**（390 / 1440 とも） |
| コントラスト未達 | light 44 | **dark 0／light 0** |

`./tools/x check` は全項目通過。

**`--only=right` は index.html の `span.tag` を1件出すが、これは誤検出。**
ヒーローのチップ（`CLAUDE CERTIFIED ARCHITECT ─ FOUNDATIONS`）は
インラインなので本文列の右端まで伸びない。追いかけないこと。

### 図の要否判定 ─ 本文の全節ぶんの結果

**図の要否判定は本文の全節ぶん合意済み。** ゴールの型（§5）で決めた結果（**済**と書いた3節は網羅の穴を埋めた回に実施）：

**Domain 2（8項）── すべて済**

| 節 | 型 | やったこと |
|---|---|---|
| 2-1 このドメインについて | 定義 | 図なし（ゴールに client / server を追記） |
| 2-2 3つの部品 | 対応 | 図 ─ 3段のカード。`description` だけ `.key` |
| 2-3 ツールの粒度 | 区別 | 比較表 ─ **どこで間違えるか**（選ぶところ／引数を埋めるところ）を軸にした |
| 2-4 失敗は読める形で | 対比 | コード対比 ＋ 既存の `pre.code`（`is_error`） |
| 2-6 MCP という差込口 | 構造 | 図 ─ **N×M の網は描かず**、3×3／5×5／10×10 の本数の伸びを帯で見せた。3層は `.fig-row.flow` |
| 2-8 権限と副作用 | 区別 | 比較表2つ（読む／変える、弱い／強い）。小見出しを1つ増やした |
| 2-9 誰にどのツールを配るか | 対応 | 図 ─ 下にいくほど壊せる範囲が広がる3枚 |
| 2-10 組み込みツール | 区別 | 図＋比較表2つ（client / server、書く量の少ない順、`Bash` との権限差） |

**2-6 で網を描かなかった理由**：交差する線は「形そのものが情報」に見えるが、
390px では読めず、しかも**伝わるのは「多い」までで「増えるほど差が開く」が出ない**。
本数の伸びを数字で並べたほうが速い。

**Domain 3（9項）── すべて済**　3-1 図なし（6タスク一覧の図を削除し、代わりに「読ませるもの／通さないもの」の
線引きを置いた）／3-2 図（効く範囲の順に積む＋連結の流れ）／3-3 コード＋比較表（パスの3書式）／
3-5 コード＋図（`context: fork` の対比、4択は `.fig-steps` の分かれ道）／3-7 コード＋図（`paths` あり／なし）／
3-8 図（承認が入る位置の対比）／3-9 図（一発 ⇄ 小さく回す＋書き戻し先）／3-11 コード＋比較表2つ／3-12 比較表2つ＋コード

**Domain 3 で色を減らした。** 元は5つの置き場所に4色、3つの選択肢に3色を当てていて、
赤＝危険・緑＝OK の対応が壊れていた（§7 #22）。いまは中立に統一し、
強調は `.key` と `.fig-lbl` の軸ラベル（「↓ 効く範囲が広い」）で出している。

**Domain 4（11項）── すべて済**　4-1 図なし／4-2 図（幅＝効く範囲）＋比較表／4-3 図（後ろほど効く）／
4-4 比較表2つ／**4-5 は削除ではなくシーケンス図にした**（下記）／4-7・4-8 テキスト例（`pre.code` の対比）＋比較表／
4-10 コード対比＋比較表／4-11 比較表＋`.fig-steps`／4-13 図＋コード＋比較表／4-14 図＋比較表

**判定表から1つだけ外れた ─ 4-5。** 判定は「図を削除（thinking は形そのものが情報なのでコード対比へ）」だったが、
削除対象に見えていた429字の図は **thinking の JSON ではなく、ストリーミングのイベント列**だった。
形ではなく**やり取りの順序**が情報なので、§5 の「やり取りは原則シーケンス図」に従って `.fig-seq` 2本に置き換えた
（`stream` なし／`stream: true`）。前半の JSON はもともと `pre.code` で、判定の意図はそちらで満たされている。
あわせて、実装のない `data-widget="p-seq2"` を削除した。

**Domain 5（8項）── すべて済**　5-1 図なし／5-2 図（帯＋2段の道）／5-3 図（3段の流れ＋子に渡す3つ）／
5-4 比較表（確率的⇄決定的）／5-6 図（そのまま返す⇄返す前に形を決める）／5-7 図＋比較表（引き金）／
5-8 比較表（残すもの⇄残していないと）／5-9 図（1体だけ失敗）

**まとめ ── すべて済**　6-1 図（1周を左から右へ＋外から効く2つ）／6-2 配点の帯（`<!--#weightfig-->`）／模擬は図なし

---

## 11. 未着手・今後

- ~~設問数がドメインの比率と釣り合っていない~~ **── 解消した。** 配点のある5ドメインで**差 0**：

  | ドメイン | 配点 | 設問 | 期待 | 差 |
  |---|---|---|---|---|
  | 1 エージェント設計 | 27% | 27 | 27 | 0 |
  | 2 ツールと MCP | 18% | 18 | 18 | 0 |
  | 3 Claude Code | 20% | 20 | 20 | 0 |
  | 4 プロンプト設計 | 20% | 20 | 20 | 0 |
  | 5 コンテキスト管理 | 15% | 15 | 15 | 0 |

  ほかに前提4問・まとめ25問（模擬試験⑦本を含む）で**全129問**。
  **足すときは Domain 3（20問＝20%）を基準に総数を出す** ── 不足分をそのまま足すと
  総数が増えて期待値も動くので、いつまでも合わない（§7 #40）。
  釣り合いは `./tools/x node` で数え直せる（`docs/*/*.html` の `data-quiz` と `quiz-data.js` を突き合わせる）。
- ~~`check.mjs` の入れ残し~~ **── 解消した。** 検査1の走査を本文の inline style と SVG 属性へ広げ、
  5k「図の中の文の長さ」を追加。検査2（`text` の座標）は SVG 全廃で出番がなくなったので削除、
  検査4は SVG の `<text>` ではなく**同じ `.figbox` の中の文とコード内コメント**と突き合わせる形に直した。
  **全28項目が「対象が何件あったか」を出す**ので、通ったのか見ていないのかが区別できる。
  「`.figbox` の外に `.ann`」は変換完了で不要（残る13件はすべて `pre.code` の注記で、5j が見ている）
- ~~公式タスク名の英語原文~~ **── 併記ではなく原文のみにした**（§2・§8）。日本語訳は廃止。
- ~~模擬シナリオ⑤の扱い~~ **── 残す。** 公式 Exam Guide でシナリオ6本を確認したところ、
  教材の7本は**公式6本すべてに近いものを持っている**（① Customer Support ／ ② Multi-Agent Research・
  Developer Productivity ／ ③ Code Generation with Claude Code ／ ④ Structured Data Extraction ／
  ⑦ Claude Code for Continuous Integration）。⑤ MCP と ⑥ 一括移行は**追加の演習**として残す。

### いま残っているのはこれだけ

- **設問がすべて「1つ選ぶ」形になっている。** 公式 Exam Guide の Item format は
  `Multiple-choice and multiple-response items; each item states how many responses to select` ──
  **本番には「複数選ぶ」設問が混じる**（§7 #43）。
  いまはトップと 6-4 に「本番には複数選ぶ設問がある」と書いて注意を促しているだけ。
  対応するなら `quiz-data.js` に複数正解（`a` を配列にする）を持たせ、`app.js` の採点を
  「選び終えてから判定」に変える必要がある。**やるかどうかは利用者の判断待ち。**

---

## 12. 利用者について（作業の進め方）

- **黙って進めない。**着手前に「やること」を本文の箇条書きで示し、長い作業では区切りごとに報告する
- **タスクリストのウィジェットは相手の画面に表示されていない。**進捗は必ず本文テキストで書く
- **判断が分かれた作業では**、迷った点・選ばなかった案とその理由を短く添える（毎回の実況は不要）
- **頼まれていない副産物を作らない。**メモや md を勝手に増やさない
- 指摘は的確。「この図はどれのこと？」「なぜ 2.1 が 1-2 にあるのか？」「色を濫用しすぎでは？」といった
  問いは、**たいてい当たっている。**反射的に弁明せず、まず測って確かめること。
  これまで指摘されたものは全件、実際に問題があった
